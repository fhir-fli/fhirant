name: fhirant
description: A FHIR server application using Shelf
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  fhir_r4: 
    path: ../../fhir_r4
  flutter:
    sdk: flutter
  flutter_background_service: ^5.0.10
  flutter_local_notifications: ^18.0.1
  flutter_localizations:
    sdk: flutter
  http: ^1.2.2
  intl: ^0.19.0
  path: ^1.9.0
  path_provider: ^2.1.5
  shelf: ^1.4.0
  shelf_router: ^1.0.0
  shelf_static: ^1.0.0
  sqlcipher_flutter_libs: ^0.6.4
  sqlite3: ^2.5.0

dev_dependencies:
  build_runner: ^2.4.13
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/

# fhirant review, 2026-09-17 (third fresh review)

**54 findings: 34 confirmed by a probe or a measurement that ran, 20 by reading only (marked; 8 of those are the structure notes).** None is fixed yet. Nothing in fhirant has been changed; the only files added are this one and `packages/fhirant_server/tool/review_2026-09-17/` (probes, their `.tsv` results and logs).

**Method.** Inventory-driven: all 119 hand-written Dart files under `lib/` and `bin/` of fhirant's five packages, `fhir_db`, and the `fhir_r4_db` binding were listed first and read whole (appendix A, every row ticked). The two generated files of the binding (`search_parameters.dart` 35,002 lines, `search_parameter_types.dart` 13,227) were not read line by line; they were covered by the 2026-09-13 census and are generator output. The three earlier review files were not opened until the reading and probing were finished; section 8 is the comparison. Security was reviewed with authentication ON. Authentication off by default during testing is settled and is not a finding here.

Every probe writes one line per case as it finishes:

| file under `tool/review_2026-09-17/` | what it holds |
|---|---|
| `http_probe.tsv` | 22 requests through the full pipeline, auth on: 20 findings, 2 as expected |
| `backup_roundtrip.tsv` | R1, seven steps |
| `store_probe.tsv`, `store_probe2.tsv` | SQLite build and PRAGMAs, hash cost, rebuild, forecast, export file, journal modes, time zone |
| `fhirpath_cost.tsv`, `search_modifier.tsv`, `timer_error.log`, `docker_smoke.log` | as named |
| `probes/*.dart.txt` | the probes, kept as text so CI's format and analyze steps pass over them; copy one to `.dart` and run it with `dart test <path>` from `packages/fhirant_server` |

Severity: 🔴 a record is lost, leaked, or a clinical answer is wrong and looks right · 🟠 wrong or unsafe under conditions that will occur · 🟡 conformance, hygiene, cost.

---

## 1. The ten to fix first

| # | | finding | evidence |
|---|---|---|---|
| R1 | 🔴 | No backup made by the CLI or the app can be restored | `backup_roundtrip.tsv` |
| F1 | 🔴 | The forecast answers 200 with recommendations when the immunization history could not be read | `store_probe.tsv` FC1 |
| S3 | 🔴 | A client-written `meta.tag` removes a resource from every `$backup` and system `$export` | `http_probe.tsv` BK3 |
| Q1 | 🔴 | A date with no zone is indexed in the device's zone; a phone that changes zone stops matching `birthdate=` | `store_probe2.tsv` Q10 |
| Q2 | 🔴 | During `$reindex` searches answer from a half-built index (14 different counts, and SQL errors, for one query) | `store_probe2.tsv` F1 |
| T1 | 🔴 | A CodeSystem the server does not hold is treated as empty: `$expand` 200 with nothing, `$validate-code` false, `:in` nothing, `:not-in` everything | `http_probe.tsv` T3 |
| A1 | 🔴 | patient/ scopes with no patient context read every patient through `GET /?_type=` | `http_probe.tsv` A7 |
| A2 | 🔴 | Any writer can rewrite and delete AuditEvents | `http_probe.tsv` AU1 |
| A3 | 🟠 | Five wrong passwords from anyone end the account's live sessions, every 15 minutes, indefinitely | `http_probe.tsv` A3 |
| A4 | 🟠 | An unregistered OAuth `client_id` with any `redirect_uri` is issued a code | `http_probe.tsv` A20a |

---

## 2. Records lost, or clinical answers wrong

**R1. 🔴 No backup made by the CLI or the app can be restored. CONFIRMED.**

> ✅ **FIXED 2026-09-17** (`2e867b8` and the two commits after it). `copyEncrypted` writes one format, SQLCipher 4, from any store; `restoreEncrypted` finds the file's `legacy` setting by opening it (4, then 0), so backups written before the fix by either kind of store restore. `store_cipher.dart` is the one place the store's cipher configuration is written; the CLI and the app call it. **The end-to-end check found a second defect (R1b):** `$restore` over a real socket answered 500 before opening the file, because `request.read().pipe(IOSink)` throws a TypeError when the body is a `Stream<Uint8List>`, which it is behind `dart:io` and never is in an in-process handler test. Fixed, with the first test in the suite that goes through a socket. Two running CLI servers, backup from one, restore into the other: 400 on the wrong passphrase, 200 and the Patient readable on the right one. Evidence: `packages/fhirant_server/tool/review_2026-09-17/fix_r1/`. D1's probe can now run.
`bin/server.dart:126` and `database_service.dart:67` open the store with `PRAGMA legacy = 4`. `copyEncrypted` attaches the backup on that connection, so the file is written with the legacy-4 parameters. `restoreEncrypted` (`fhirant_db.dart:503-505`, and the upgrade open at `:522-525`) opens it with `cipher` and `key` only; SQLite answers "file is not a database" and the operator is told the passphrase is wrong.

| step | result |
|---|---|
| backup from a store opened as production opens it | written, 131,072 bytes |
| raw open the way `restoreEncrypted` does (no `legacy`) | **fails: file is not a database** |
| raw open with `PRAGMA legacy = 4` added | opens, `user_version=23`, 1 resource |
| `restoreEncrypted` into the same store, and into a fresh production-configured store | **BackupUnreadable**, both |
| the same round trip from a plain in-memory store (what every backup test runs) | restored 1 |

The backups are not damaged: they open with `legacy = 4`. The restore path is what fails, for every backup a real install has made. Every backup test builds its store with `NativeDatabase.memory()` and no cipher pragmas, which is why none saw it.
Fix: one function that applies the cipher configuration (`cipher`, `legacy`, `key`), used by the CLI, the app, the ATTACH in `copyEncrypted` and both opens in `restoreEncrypted`; a backup test whose source store is opened exactly as `bin/server.dart` opens it; restore also tries `legacy = 4` for files written before the fix.

**F1. 🔴 The forecast swallows a failed read of the history. CONFIRMED.**

> ✅ **FIXED 2026-09-17.** `_searchForPatient` no longer catches; a failed Immunization, Condition or AllergyIntolerance read is a 500 with an OperationOutcome and no recommendation, on both request shapes (6 tests, 0 of 6 before). Checked at the handler with a store that throws; not induced through a running CLI, which has no fault to inject. Evidence: `packages/fhirant_server/tool/review_2026-09-17/fix_f1/`.
>
> **F1b, found fixing it, FIXED the same day.** `cql_handler._loadPatientData` had the same catch around each of its ten patient reads, "to skip resource types that don't have a patient search param". All ten have one (`fhir_generator/fhir_r4/definitions.json/search-parameters.json`), so the catch only ran CQL on a partial record when a read threw. Now a 500. Every other catch in the server that returns empty, null or `continue` was read (8 sites): token decode, body parse, Library content parse; none is a store read.
`forecast_handler.dart:212-227`, `_searchForPatient`: `catch (_) { return []; }`. With every search throwing, `$immds-forecast` answered **200 with an ImmunizationRecommendation** (56,712 bytes). A failed Immunization read becomes "no doses given", so every dose is due; a failed Condition or AllergyIntolerance read drops the contraindications. Nothing in the response says so. Fix: let it throw; 5xx.

**S3. 🔴 The `spec` tag is client-writable, and it decides what a backup contains. CONFIRMED.**

> ✅ **FIXED 2026-09-17** (fhir_db `ea38966`, then fhirant). The tag merge lives in the `fhir_db` core, so the rule does: `FhirDao.serverOwnedTags`, and a save without `asServer` neither adds such a tag nor keeps one. `FhirAntDb` declares the `spec` tag in its constructor (`fhirant_db/lib/db/spec_tag.dart`), so every store has the rule however it was opened; the specification load is the one `asServer` writer. Seven tests through the full pipeline (PUT, POST, `$meta-add`, transaction Bundle, JSON Patch, the load itself, a client write over a specification resource): 1 of 7 before. Two running CLI servers: a Patient PUT with the tag is stored without it and both of 2 patients restore. Evidence: `packages/fhirant_server/tool/review_2026-09-17/fix_s3/`.
>
> Decided with it: a client write over a specification resource takes the tag off, so the deployment's change is backed up rather than silently left out. **S3b, ✅ FIXED 2026-09-17, and my first statement of it was wrong.** I wrote that a restore is followed by the specification reload. In the CLI it cannot be: the load is awaited before the server serves (`bin/server.dart:137`). The real window: the load runs whenever the store holds no CodeSystem, so it runs again after a load interrupted before `valuesets.ndjson` (the last file), and the app runs it unawaited beside a live server (`main.dart:75`). It saved over whatever it found, so a resource a client had written by then became history under the shipped copy, tagged as the server's again. The load now asks the store which of each chunk it already holds (`FhirAntDb.heldKeys`, a primary-key lookup: `fix_s3b/03_query_plan.log`) and saves only the rest: it fills what is missing and overwrites nothing. Two tests, both failing before. CLI: the real load takes 14.4 s for 4,213 resources; an edited CodeSystem stands across a restart, version 2, untagged (`fix_s3b/07_cli_load.log`). **Not covered:** the SQLite-file restore copies rows, so a crafted backup can carry the tag in; `$restore` is admin-only. Resources a client tagged before this fix keep the tag until their next write; nobody but us has such a store.
`$backup` and system `$export` leave out every resource whose `meta.tag` carries `http://fhirant.fhir-fli.dev/CodeSystem/tags|spec` (`spec_loader.dart:27-36`, `copyEncrypted(withoutTag:)`, `BackupService.bundle`). A clinician PUT a Patient carrying that tag; the backup restored **1 of 2 patients**. The same shape as the DELETED-tag tombstone of 2026-09-08 row 36: a server decision keyed on a field a client writes. Fix: a server-owned column (or refuse/strip the tag on any write that is not the specification load). This does not reopen where the specification lives (decided 2026-09-08: in `resources`); it is about what marks it.

**Q1. 🔴 Dates with no zone are indexed in the device's local zone. CONFIRMED.**

> ✅ **FIXED 2026-09-18** (fhir_db `d4e2845`; bindings `53910aa14` / `baf0c154` / `28a594aa`). One branch in `FhirDateValue.tryParse`: a zone-less value is a UTC wall clock, on both sides. The bindings carry a two-process test: the store written under America/New_York, searched by children started under Asia/Tokyo, Pacific/Honolulu and UTC; `birthdate=1990-01-15` found 0 of 1 before, 1 of 1 after, and a zoned value is 1 of 1 everywhere. Decided with it: a zone-less value against a zoned one is compared on the UTC clock (the page says nothing of that case). Six existing tests that asserted the local clock now assert UTC. Suites: fhir_db 37, bindings 474 / 477 / 475, server 1,154. **A store indexed before this needs `$reindex`**, the same rebuild Q8 needs. Evidence `packages/fhirant_server/tool/review_2026-09-17/fix_q1/`.
`fhir_date.dart:100-110`. Indexed under `TZ=America/New_York`, `Patient?birthdate=1990-01-15` matched 1; the same file searched under `TZ=Asia/Tokyo` matched **0** (`eq` is containment, and the stored day no longer sits inside the searched day). A phone that travels, or whose zone setting changes, silently loses equality matches on every zone-less date: every `birthDate`, every date-only `onset`, `effective`, `issued`. R4B search.html 3.1.1.4.7 asks for the server's local zone "Where both search parameters and resource element date times do not have time zones"; a server whose zone moves cannot do that with stored instants. Fix: index and search zone-less values in one fixed zone (UTC) on both sides, so the comparison is zone-independent; the rebuild re-extracts every row.

**Q2. 🔴 Searches during `$reindex` read a half-built index. CONFIRMED.**

> ✅ **FIXED 2026-09-18** (fhir_db `e7446be`; bindings `981e14868` / `99ca6edd` / `308271c5`). The rebuild builds the nine tables again as `<table>_rebuild` beside the live index, then one transaction drops the old tables, renames the new ones in, creates the value indexes under their own names, re-extracts every resource saved since the rebuild began and drops the rows of every resource deleted since. Searches wait for that transaction and never read between its steps. Decided on the way: canonical index names (an index cannot be renamed and the migration steps drop them by name), so the index build sits inside the swap transaction and is the blocking window. An event-loop turn between pages keeps the CLI's same-isolate server serving. Binding tests: 3,000 Patients on a background-isolate store polled through the rebuild, **6 different answers before, one after**; a save, an update and a delete made during the rebuild right after it (a save during it used to fail on the missing table); indexes once each after two rebuilds. Running CLI, store on the server's isolate: **40,040 resources rebuilt in 9.3 s with 21 polls of `gender=female` answered, all 20,000, longest 2.28 s**; before, not one poll got in (`fix_q2/09_cli_reindex_polled.log`). Suites: fhir_db 37, bindings 477 / 480 / 478, server 1,154.
`rebuildSearchIndex` (`fhir_db.dart:493-582`) drops the nine tables, refills them 500 resources at a time, and creates the indexes last, none of it in one transaction. With the store on its own isolate (the app's configuration), `gender=female` over 6,000 Patients polled during a 2.5 s rebuild gave **14 different answers from -1 (SQL error: table missing) to 6,000**. `reindex_handler.dart:20` puts the 929k-resource rebuild at about 1,000 s. With the store on the server's isolate (the CLI) the same rebuild instead holds the whole server: my first probe could not get one poll in. Fix: build into shadow tables and swap them in one transaction; or answer 503 to searches while it runs.

**T1. 🔴 "This server does not hold that CodeSystem" is answered as "no such code". CONFIRMED.**

> ✅ **T1 and T2 FIXED together 2026-09-17** (fhir_db `5598931`, bindings' tests, then fhirant). They were one defect: three copies of the value set expansion (`$expand`, `$validate-code`, the store's `:in`), each with its own rules, which is also how the 2026-09-06 refusal reached two of them and not the third. There is now one, `FhirDao.expandValueSet`, and all three use it. It refuses, with a `ValueSetRefusal`: a ValueSet not held; a whole-CodeSystem include whose CodeSystem is not held, not held in the `include.version` asked for, or held with `content` other than `complete`. Responses: `$expand` and `ValueSet/$validate-code` 422; `:in`/`:not-in` 400; `$validate-code` against a CodeSystem or ValueSet url not held 404 (was 200 `result=false`); a code missing from a CodeSystem held as a fragment 422, a code it lists still valid. Issue code `not-found` for what is not held, `not-supported` for what is not implemented or held in part.
>
> Sources, read whole 2026-09-17: R4B valueset-operation-expand.html ("...then it SHALL return an error"); codesystem-operation-validate-code.html (its error example: "when the server is unable to determine whether the code is valid"); the OperationDefinitions' out parameters are result, message, display, so tx.fhir.org's `issues` / `x-caused-by-unknown-system` shape (200 with result=false) is not R4B and was not followed. search.html says nothing of a value set that cannot be expanded: refusing the search is our reading. **Deliberately not refused:** an include that lists its concepts, against a CodeSystem not held; the list is the answer, and it is what makes `:in` usable on a device that cannot hold LOINC. tx.fhir.org refuses that too (probed 2026-09-17).
>
> Also fixed on the way: compartment search and a Bundle entry's `ifNoneExist` did not catch the store's value set refusals at all (500). Five older handler tests that mocked the store now use a real one; their expectations did not change. Tests: 16 per binding (13 failing before), 10 in fhirant through the pipeline (9 failing before). Suites: fhir_db 37, bindings 467/470/468, server 1,149. Evidence `packages/fhirant_server/tool/review_2026-09-17/fix_t1_t2/`.
>
> **L1, found by the CLI check, ✅ FIXED 2026-09-17** (fhir_db `ab736ba`, then fhirant). The CLI's specification load stopped at `search-parameters.ndjson` on the store's own `InvalidSearchParameter`, so `valuesets.ndjson`, which sorts after it, never loaded: a CLI server held **0 CodeSystems, 0 ValueSets, 0 SearchParameters** (`fix_t1_t2/12_cli_with_spec.log`). Since fhir_db 0.14.0 (2026-09-14) every SearchParameter save is checked as a client's upload; the shipped definitions are not uploads (8 of 1,414 have no expression, the rest define built-in codes). A save made `asServer` now skips that check; a client's is checked as before. The loader tests fed the loader resources they wrote themselves; `spec_loader_real_files_test.dart` loads the shipped file. After: 4,213 loaded, 540 CodeSystems, 721 ValueSets, 1,414 SearchParameters, and with them held every T1 answer is right (`fix_l1/05_cli_with_spec.log`). The app's asset load goes through the same `loadSpecLines`; not run on a device.
>
> **Q8, found by the same run, ✅ FIXED 2026-09-18** (fhir_generator `975e560` + `37cc4b9`; fhir_r4 `0b73e0818`/`c7a06be32`, fhir_r5 `75fc1a3a`, fhir_r6 `7cd91e8`; fhirant test). The cause was two generator defects, not the index: every enum constant's `system` was the **ValueSet's url**, not a code system, and a value parsed from JSON carried no system at all, so `fhir_r*_db`'s `enumSystem` (which already existed) had nothing to read. Constants now carry the CodeSystem and its version; a parsed value takes its constant's system, version and display; `withElement` keeps them; `toJson` is unchanged. Enums regenerated for R4, R5 and R6 through a new `bin/regen_enums.dart`, after a regeneration with the unchanged writers showed the enums held no hand-fixes (formatting, an ignore header, and one Builder display the writer could not emit: a third generator defect, quotes in a display string, fixed too). Suites: cores 19,144 / 18,350 / 19,352; mappings 98 ×3; bindings 472 / 475 / 473; fhirant server 1,154. **A store indexed before this needs `$reindex`** for `status=<system>|code`, `:in` and `:not-in` on code elements to match; single-code searches were never affected. The earlier claim that "the fix is in the generator (the system of each code element's bound value set)" was half right: the generator, but the enum's own system, not the search extractor.
A ValueSet including all of `http://loinc.org`: `$expand` **200, total 0**; `$validate-code` of LOINC 8867-4 **200, result=false**; `Observation?code:in=` **0**; `code:not-in=` **every Observation**. `terminology_handler.dart:412-415, 716-726` and `fhir_dao.dart:4282-4296` skip a CodeSystem that is not found. The 2026-09-06 rule (finding 24: refuse a compose the server cannot evaluate) was applied to `filter` and `valueSet` and not to this. Also unhandled: a CodeSystem present with `content` other than `complete`. Fix: 422 / 400 `not-supported`, as finding 24 does.

**T2. 🟠 `$validate-code` does not apply `compose.exclude`. CONFIRMED.**

> ✅ **FIXED 2026-09-17 with T1**, see there: `$validate-code` validates against the same expansion `$expand` returns.
Include all of a CodeSystem, exclude code `a`: `$expand` lists `[b]`, `$validate-code` of `a` answers **true**. `terminology_handler.dart:697-726` returns on the first include that matches; the comment at `:729` says the excludes need no check. REVIEW-2026-09-08 line 30 lists this as "Refuted by reading". It was read wrong; the probe is the correction.

**D1. 🟠 Restore over a newer local version regresses the resource and then overwrites its history. BY READING** (the probe could not run until R1 is fixed).
`restoreEncrypted` does `INSERT OR REPLACE INTO resources` from the backup (`fhirant_db.dart:575`): a local v3 is replaced by the backup's v1, and v3 is not moved to history. The next saves count v2, v3 again, and `saveResource` upserts history by `(type, id, version)` (`fhir_dao.dart:247`), replacing the local v2 and v3 rows.

> ✅ **FIXED 2026-09-19.** A resource the store already holds is restored through the ordinary write: a new version whose content is the backup's, the local current version moved to history, later saves counting on from there; ids only in the backup keep the bulk path (`INSERT OR IGNORE`). Test: three local saves, a backup carrying v1 → content is the backup's, versionId 4, history 4/3/2/1, the next save is 5; fails without the change (`fix_d1/02_fails_without_fix.log`). Server suite 1,234, db 129.

---

## 3. Security, with authentication on

**A1. 🔴 patient/ scopes with no patient context are unconfined at the root. CONFIRMED.**

> ✅ **FIXED 2026-09-18** with A2, A3, A5, A6 as one change to `authorizeRequest`, which the middleware and every Bundle entry go through. The patient-context check now runs before anything else, at the root as at a type: `GET /?_type=Patient`, `/_history`, `POST /_search` with `patient/*.rs` and no patient are 403. Not done: refusing such an account at `/auth/register` or `/auth/login`; the token is useless now, which is the point, and an account an administrator created with `patient/` scopes and no patient is the administrator's to fix. Tests through the pipeline in `authorization_table_test.dart` (11, 6 groups failing before). Server suite 1,165. Evidence `packages/fhirant_server/tool/review_2026-09-17/fix_auth/`.
`Principal.compartmentFor` returns null (no confinement) when the token has no `patient` claim (`request_authorization.dart:85-86`). `authorizeRequest` refuses such a token only on typed paths (`:175-180`). `GET /?_type=Patient` with scopes `patient/*.rs` and no patient: **200, total=2**, both patients. `/auth/register` lets an administrator create that account (scopes `patient/*.rs`, no `patient`), and `/auth/login` issues the token as the account has it. Fix: a patient-context scope with no patient context fails closed in `compartmentFor` and at login.

**A2. 🔴 The audit trail is writable by the people it records. CONFIRMED.**

> ✅ **FIXED 2026-09-18**: every client write of an AuditEvent (create, update, patch, delete, `$meta-add`, `$meta-delete`, conditional or not, REST or Bundle entry) is 403, the administrator's included; the server writes its records straight to the store. Reads unchanged.
A clinician (`user/*.cruds`): `PUT /AuditEvent/{id}` with another agent and outcome **200**, `DELETE` **204**. Fix: AuditEvent is append-only to everyone but the server; refuse PUT, PATCH, DELETE and client POST in `authorizeRequest` and in Bundle entries.

**A3. 🟠 Account lockout ends live sessions and can be held indefinitely by anyone. CONFIRMED.**

> ✅ **FIXED 2026-09-18**: the middleware no longer refuses a valid token while the account is locked; a lock blocks password login only. Deactivation still ends sessions. This reverses REVIEW-2026-09-08 row 14's test, which asserted that a lock ends sessions; that test now asserts the opposite and says why. The half about anonymous callers locking a known username at will is the trade-off of a lockout and stands; it no longer costs a session.
The middleware refuses a valid token while the account is locked (`auth_middleware.dart:110-113`), and five wrong passwords lock any known username (10 credential requests a minute are allowed per address). Before: 200. After five anonymous wrong logins: **401** on the victim's valid token. Repeated every 15 minutes it keeps the only administrator out. Fix: a lock blocks password login; it does not revoke sessions.

**A4. 🟠 OAuth: any `client_id`, any `redirect_uri`, first use wins. CONFIRMED.**
An unregistered `growth-chart` with `redirect_uri=https://evil.example/cb` and the user's password: **302 to evil.example with the code**; the attacker chose the PKCE challenge, so the attacker holds the verifier. The consent page names the `client_id` the attacker typed and not the redirect host. Trust-on-first-use (`authorize_handler.dart:432-435`) also lets any account holder pin a well-known client id to their own URL. Fix: clients are registered by an administrator; an unregistered client is refused, or limited to loopback and private-use schemes; show the redirect host.
Also CONFIRMED: `Uri.replace(queryParameters:)` drops the redirect URI's own query (`?keep=1` was lost); RFC 6749 3.1.2 requires it kept.

**A5. 🟠 HEAD is never scope-checked. CONFIRMED.**

> ✅ **FIXED 2026-09-18**: `methodToPermission` maps HEAD as GET; `HEAD /Patient/a` with `user/Observation.rs` is 403.
`methodToPermission` has no case for HEAD (`smart_scopes.dart:192-231`), so the middleware checks nothing, and shelf_router serves HEAD from every GET route. Token `user/Observation.rs`: `GET /Patient/a` **403**, `HEAD /Patient/a` **200** (with ETag and Last-Modified). Compartment checks inside handlers still run; the type scope does not.

**A6. 🟠 A SearchParameter can be deleted without system authority. CONFIRMED.**

> ✅ **FIXED 2026-09-18**: the rule moved from the instance delete handler into `authorizeRequest`, so the conditional delete and a Bundle entry answer the same: 403 for a clinician, 204 for an administrator.
`DELETE /SearchParameter/id` as a clinician: 403. `DELETE /SearchParameter?code=…`: **204** (then 410 on read). A Bundle `DELETE` entry: **204**. The check lives in `deleteResourceHandler` only.

**A7. 🟠 REST PATCH skips the Subscription service. CONFIRMED.**

> ✅ **FIXED 2026-09-18**: `patchResourceHandler`, `metaAddHandler` and `metaDeleteHandler` take the server's `SubscriptionService`; a PATCHed Subscription goes through `activate` (a client's `active` on one the server set `error` stays `error`), and every PATCH, `$meta-add` and `$meta-delete` notifies matching subscribers. Three handler tests with a recording HTTP client (`patch_and_meta_notify_test.dart`). Server suite 1,168.
`patchResourceHandler` takes no `SubscriptionService` (`fhirant_server.dart:552-556`). A Subscription the server had set to `error` (unsupported channel) was PATCHed to `active` and stored **active**: the client decides its own status. A PATCH of any other resource notifies no subscriber. The Bundle PATCH path does both. `$meta-add` and `$meta-delete` also notify nothing.

**A8. 🟠 Bulk export writes the record to disk in plaintext. CONFIRMED.**

> ✅ **FIXED 2026-09-18, not as proposed.** The proposal was to stream from the store at download time and write no file. Bulk Data 2.0.0 export.html, read whole 2026-09-18, on `transactionTime`: "The response SHOULD NOT include any resources modified after this instant, and SHALL include any matching resources modified up to and including this instant". A download re-run against the live store would include later edits and lose later deletions, so the files are the snapshot and stay. They are now encrypted at rest: each job mints a random 256-bit key held in its `export_jobs` row inside the encrypted store (schema 24) and nowhere else; the file is framed AES-256-GCM (`export_file_crypto.dart`: 64 KiB frames, per-frame nonce and tag, the header and a last-frame flag as associated data), written and served in bounded memory. `$export-file` reads the plaintext length from the frame headers for `Content-Length` and checks the first frame before the response starts, so a changed or cut file is a refusal, not a partial 200. A job from before schema 24 has no key and its files answer 410. Tests: three through the pipeline (plaintext absent on disk, download intact, tamper and truncation refused), six on the cipher alone. Suites: store 129, server 1,177, app 34.
After `$export`, `Patient.ndjson` under the export directory contained the patient's name, readable, and stays 24 h (`kExportRetention`). The database beside it is encrypted. In the app the directory is under Documents (`database_service.dart:78-85`), which iOS includes in device backups. `FhirAntDb.exportJson` already streams from the store: `$export-file` can stream from the store and write no file; the manifest needs only counts.

**A9. 🟠 Client-supplied programs run on the server's isolate with no deadline. CONFIRMED (cost measured).**

> ✅ **FIXED 2026-09-18.** All four run in a fresh worker isolate under `FhirAntServer.programDeadline` (default 10 s, unmeasured on a phone) through `runProgram` / `runHostedProgram` (`utils/program_sandbox.dart`); at the deadline the isolate is killed and the request is 422 `too-costly`. The kill is real: a spinning worker cost 50 CPU ticks per 500 ms and 0 after it (`fix_a9/kill_cpu.tsv`); spawn plus a FHIRPath engine is 6 + 22 ms cold, under 2 ms warm (`fix_a9/isolate_cost.tsv`), so no pool. `$transform` needs the store mid-run, so its worker holds a `HostResourceCache` that asks the server's isolate for each canonical (`utils/host_resource_cache.dart`); a host failure is a 500, the engine throwing stays a 500, only the deadline is 422. 🔑 A closure carries its whole scope's context into the spawn, so the worker program must be built in a function whose scope holds no database (`fix_a9/06_probe_error.log`). Eleven tests, including the review's expression at depth 7 through `$fhirpath` and `$transform`, and a 10^7-row CQL query. Server suite 1,195.
`$fhirpath`, `$cql`, `Library/$evaluate`, `$transform`. A 293-character FHIRPath expression (six nested `select`s over ten literals) held the handler **1,002 ms**; each further 48 characters multiplies that by ten (3, 4, 5, 6 levels: 63, 75, 120, 1,002 ms; the tenfold step is measured from 5 to 6, extrapolated beyond). While it runs, nothing else is served. Fix: evaluate in a worker isolate with a deadline and kill it.

**A10. 🟠 No request body limit. CONFIRMED.**

> ✅ **FIXED 2026-09-18.** `bodyLimitMiddleware`, before authentication: a body over `FhirAntServer.maxRequestBody` (default 16 MiB, unmeasured against any real deployment) is 413 with an OperationOutcome, from the `Content-Length` header when declared and at the byte the cap is passed when not; `$restore` is not capped, it streams to disk. Four tests. Server suite 1,184.
A 20 MB Patient was accepted: **201**. Every write route is `request.readAsString()`. On a phone this is memory.

**A11. 🟠 Password hashing blocks the server and is below current guidance. MEASURED.**

> ✅ **FIXED 2026-09-18.** New hashes are Argon2id at OWASP's minimum (19 MiB, t=2, p=1), computed in `Isolate.run`, so a login no longer holds the server isolate. Measured on this desktop (`fix_a11/kdf_bench.tsv`): the old hand-written PBKDF2 at 120k, 277 ms; PBKDF2 at OWASP's 600k, 1,816 ms; Argon2id at the minimum, 150 ms. The PBKDF2 and legacy HMAC formats still verify and are re-hashed on the next login. A test proves a timer fires on the calling isolate while a hash runs. Phone cost: unmeasured. Server suite 1,180.
One login is **290-337 ms** of synchronous PBKDF2 on the server isolate on this desktop (phone: unmeasured). OWASP Password Storage Cheat Sheet (raw markdown read 2026-09-17, section "PBKDF2", verbatim): "PBKDF2-HMAC-SHA256: 600,000 iterations (recommended)", and its summary: "Use Argon2id with a minimum configuration of 19 MiB of memory, an iteration count of 2, and 1 degree of parallelism." fhirant uses 120,000 (`password_hasher.dart:25`), in a hand-written loop that builds a new HMAC per round; `pointycastle`, already a dependency, has Argon2 and PBKDF2. Run it in `Isolate.run`.

**A12. 🟡 Login tells accounts apart. CONFIRMED.** Deactivated account **403**, unknown user **401**, locked 423, all before the password is checked; the deactivated path also skips the decoy hash (`credential_check.dart:72`).

> ✅ **FIXED 2026-09-19.** OWASP Authentication Cheat Sheet, "Authentication Responses" (raw markdown, verbatim): "an application must respond with a generic error message regardless of whether: The user ID or password was incorrect. The account does not exist. The account is locked or disabled."; and "Error Codes and URLs": a differing code "can leak information about whether the account is valid or not". `/auth/login` and both `/auth/authorize` handlers answer 401 "Invalid username or password" for all four, and the hash is computed before the account's state is looked at, so a deactivated or locked account costs what a wrong password costs. The lock still holds; the caller is not told. `test/integration/login_answers_alike_test.dart` (real Argon2id hashes; fails without the change); three older tests that pinned 403 and 423 updated. Server suite 1,237.

**A13. 🟡 Existence oracle. CONFIRMED.** For a patient token, another patient's Observation is **403**, an absent one **404**, a deleted one 410.

> ✅ **FIXED 2026-09-19.** R4B security.html, "Access Denied Response Handling" (verbatim): "Return a 404 'Not Found' - This also protects from data leakage as it is indistinguishable from a query against a resource that doesn't exist." Every READ route (read, vread, instance history, `$meta`, `$document`, `$everything`, compartment search, a Bundle GET entry) answers a resource outside the caller's compartment with the route's ordinary 404, byte for byte with the id substituted, and a deleted resource of another patient is 404 too (its compartment rows are gone, so a confined caller cannot be told it was ever there). Writes keep 403: a PUT answered "not found" would mean create. `test/integration/read_outside_compartment_test.dart` (fails without the change); eight older tests that pinned 403 for reads updated. Server suite 1,240.

**A14. 🟡 No account management.** No route changes a password, deactivates an account (`deactivateUser` exists in the store, nothing calls it), or changes role or scopes; the app can create the first administrator and nothing else. A leaked password can only be fixed in the database. BY READING.

> ✅ **FIXED 2026-09-19.** `POST /auth/password` (the caller's own password, current password checked through the one credential check, new one through the policy, answered with a fresh token pair) and, for administrators, `GET /admin/users`, `POST /admin/users/<id>/password`, `POST /admin/users/<id>/deactivate` and `/activate`, `PUT /admin/users/<id>/role`, `PUT /admin/users/<id>/scopes` (`handlers/account_handler.dart`). Every one of these changes ends the sessions from before it: OWASP Session Management Cheat Sheet, "Renew the Session ID After Any Privilege Level Change" (raw markdown, verbatim): "Common scenarios to consider include; password changes, permission changes, or switching from a regular user role to an administrator role" and "the old or previous session ID must be destroyed". Mechanism: `users.token_generation` (fhirant_db schema 25), copied into every access and refresh token as claim `gen` at issue; a change moves the account's counter on and the auth middleware and the refresh grant refuse a token whose `gen` is behind it (`auth/token_bound.dart`). A counter, not a timestamp: a token issued in the same second as the change would be ambiguous. The last active administrator cannot be deactivated or demoted (409). `test/integration/account_management_test.dart`, ten tests, all ten fail without the change. Server suite 1,250.

**A15. 🟡 First-user bootstrap is a race on the CLI and Docker.** With authentication on and no users, the first `POST /auth/register` from anyone on the network becomes the administrator. The app provisions its administrator locally; the CLI has no equivalent (an environment variable, or a one-time token printed at start). BY READING.

> ✅ **FIXED 2026-09-19.** Both, following what other servers do (their documentation read 2026-09-19). Keycloak seeds a bootstrap administrator from startup parameters (`bootstrap-admin-recovery`, verbatim: "bin/kc.[sh|bat] start --bootstrap-admin-username tmpadm --bootstrap-admin-password pass", "--password:env PASS_VAR"): here `FHIRANT_ADMIN_USERNAME` and `FHIRANT_ADMIN_PASSWORD`, applied at start through the same `AdminProvisioning` the app uses, so the password policy holds; one variable without the other, or a password outside the policy, and the server refuses to start; an existing administrator is left alone (a bootstrap, not a reset). Jenkins prints a generated one-time password to its console log and writes it to `secrets/initialAdminPassword` (installing/linux, "Unlocking Jenkins", verbatim: "From the Jenkins console log output, copy the automatically generated alphanumeric password"): here, with no variables set and no account in the store, the CLI generates a token at every start, logs it, writes it owner-only to `<db-path>/.bootstrap_token`, and the first `POST /auth/register` must carry it (`X-Bootstrap-Token` header or `bootstrap_token` body field; constant-time compare) or is 403. `GET /auth/status` reports `bootstrapTokenRequired`. Once an account exists the token opens nothing and the file is removed at the next start. The app passes no token: it provisions locally before serving, and its first registration stays as it was. `auth/bootstrap.dart`; `test/auth/bootstrap_test.dart` (8) and `test/integration/bootstrap_token_test.dart` (7; 3 fail with the check neutralized, the 4 that do not depend on it pass). `test_docker.sh` now registers through the token read from the container log and starts a second, environment-seeded container. Server suite 1,265.

**A16. 🟡 Smaller, BY READING.** Refresh rotation has no reuse detection. The authorization-code `used` check and the mark are two statements (`refresh_handler.dart:109-181`), so two concurrent exchanges both succeed. Token responses carry no `Cache-Control: no-store`. The login page has no frame or content-security headers. `Pkce` still accepts `plain`. Websocket `bind` accepts any id, any number of ids per socket, and does not check that the Subscription exists. The backup envelope's `kdf.iterations` is read from the file with no ceiling. Compartment membership ignores `reference_base_url` where `_include` checks it. Failed logins are audited as `anonymous` with no attempted username. `AuditQueue` ignores `saveResources` returning false, so a failed batch is lost without a line in the log. AuditEvents have no retention; every read writes one.

> ✅ **FIXED 2026-09-19, one commit per item, in the order listed.**
> - **PKCE `plain`**: `Pkce.verifyCodeChallenge` verifies S256 only. RFC 7636 §7.2 (verbatim): "Because of this, "plain" SHOULD NOT be used and exists only for compatibility with deployed implementations where the request path is already protected." The authorize endpoint had refused `plain` since 2026-09-07; the verifier honoured it anyway. `test/utils/pkce_test.dart` (3; the appendix B pair, `plain` with verifier equal to challenge is false; fails against the old verifier).
> - **Authorization code single use**: `FhirAntDb.consumeAuthorizationCode` marks the code used in one statement (`UPDATE … WHERE code = ? AND used = 0`) and says whether this call did it, so of two exchanges arriving together exactly one gets tokens. A second use, whether seen at the lookup or lost at the consume, is denied and moves the account's token generation on: RFC 6749 §4.1.2 (verbatim) "If an authorization code is used more than once, the authorization server MUST deny the request and SHOULD revoke (when possible) all tokens previously issued based on that authorization code." `test/integration/authorization_code_single_use_test.dart` (2: the race gives [200, 400]; a reuse is 400 and the first exchange's access token is 401 after it; both fail with the old handler).
> - **`Cache-Control: no-store`**: RFC 6749 §5.1 (verbatim) "The authorization server MUST include the HTTP "Cache-Control" response header field [RFC2616] with a value of "no-store" in any response containing tokens, credentials, or other sensitive information, as well as the "Pragma" response header field [RFC2616] with a value of "no-cache"." `utils/no_store.dart`; on login, the first registration, both `/auth/token` grants, the password change's token pair, the JSON authorize response and the 302 whose `Location` carries the code. `test/integration/no_store_test.dart` (7; all fail against the old handlers).
> - **Login page headers**: every HTML page the authorize handler serves (the form, the form shown again, the error pages) carries `X-Frame-Options: DENY`, `Content-Security-Policy: default-src 'none'; style-src 'unsafe-inline'; form-action 'self'; base-uri 'none'; frame-ancestors 'none'`, `X-Content-Type-Options: nosniff` and `Cache-Control: no-store` (`utils/html_page.dart`). OWASP Clickjacking Defense Cheat Sheet (verbatim): `frame-ancestors 'none'` "This prevents any domain from framing the content. This setting is recommended unless a specific need has been identified for framing."; `DENY` "The 'DENY' setting is recommended unless a specific need has been identified for framing." OWASP CSP Cheat Sheet (verbatim): `form-action` "restricts the URLs which the forms can submit to." The pages carry one inline `<style>` and no script. `test/integration/login_page_headers_test.dart` (3; all fail against the old handler).
> - **Refresh reuse detection**: a refresh token that was rotated away (or revoked) and is presented again moves the account's token generation on, ending the active refresh token and every access token; the check now runs after the signature and the account, so only a token this server issued for a live account can end its sessions. RFC 9700 §4.14.2 (verbatim): "The authorization server cannot determine which party submitted the invalid refresh token, but it will revoke the active refresh token. This stops the attack at the cost of forcing the legitimate client to obtain a fresh authorization grant." `test/integration/refresh_reuse_test.dart` (2: the reuse ends the rotated-to refresh token and its access token, a fresh login works; a forged token whose hash sits in the revoked table ends nothing; the first fails with the old handler).
> - **Websocket `bind`**: R4B subscription.html 2.46.8.2 (read whole, verbatim) defines "Server responds with a "bound :id" message to acknowledge." and no refusal, so a bind that is not accepted is not acknowledged. Accepted when the id is a FHIR id, the store holds that Subscription (`WebSocketSubscriptions(exists:)`, wired to `getResource` in the server), and the socket holds fewer than `maxBindsPerSocket` (16, a chosen ceiling) distinct ids; binding an id again is fine. `test/services/websocket_subscriptions_test.dart` +4 (grammar, cap and re-bind, existence with a stub, existence against the real store; all four fail with the checks neutralized). **Reverted the same day (Grey's ruling):** `/ws` already needs a token, pings carry no data, the cap of 16 was a chosen number, and the one exposure worth guarding (binding another user's Subscription) was not addressed by it.
> - **`kdf.iterations` ceiling**: `BackupCrypto.maxKdfIterations` = ten times what the server writes (2,100,000); a restore whose envelope names more is refused before any derivation. Measured (`tool/review_2026-09-17/fix_a16/kdf_cost.tsv`): 0.48 s at 210,000, 4.7 s at 2,100,000, linear, so a crafted 2³¹ would hold the server's isolate about 80 minutes. `test/utils/backup_crypto_test.dart` +1 (2³¹ refused in under a second; without the check the same test is still deriving when killed after two minutes; `timeout 20` sent SIGTERM, which `dart test` answers "Waiting for current test(s) to finish", so the kill had to be SIGKILL).
> - **Compartment membership and `reference_base_url`** (fhir_db, `_compartmentParamCondition`): with `serverBaseUrl` known, a resource whose reference is `Patient/p1` on another server is not in this server's `Patient/p1` compartment, the rule `_referenceCondition` and `referenceTargets` (`_include`) already applied; without a base the store admits it, as the reference search does. `fhir_r4_db/test/search_compartment_test.dart` +1 (fails against the old core); r5 and r6 compartment tests and the three binding suites re-run. fhir_db 0.14.0 CHANGELOG carries the entry (still unpublished).
> - **Failed logins in the trail**: a refused login left no AuditEvent at all (a 401 without an Authorization header was skipped as a bare refusal since 2026-09-07). `/auth/login` and both authorize POST paths now put the claimed name in the response context, the middleware records that refusal (the credential endpoints are rate limited, so the write is bounded), and the event carries it in `agent.name` (R4B `AuditEvent.agent.name`, profiles-resources.json: "Human-meaningful name for the agent") and `agent.who.display` with no identifier. `test/integration/failed_login_audit_test.dart` (3: wrong password, unknown account, a bare unauthenticated read still unrecorded; the first two fail with the old middleware).
> - **`AuditQueue` and a refused batch**: `saveResources` answering false is now an error in the log naming the batch size ("the store refused the batch"); a throw was already logged. `test/middleware/audit_queue_test.dart` (3: false logged, true silent, throw logged and the queue survives; read back from the logger's own file; the first fails with the old queue).
> - **AuditEvent retention**: the hourly sweep removes AuditEvents older than `FhirAntServer.auditRetention`, with their history and index rows and no tombstone, through fhir_db's new `purgeResourcesLastUpdatedBefore` (one type, one cutoff; test in `fhir_r4_db/test/purge_test.dart`). Default six years: 45 CFR 164.316(b)(2)(i) (law.cornell.edu, verbatim) "Time limit (Required). Retain the documentation required by paragraph (b)(1) of this section for 6 years from the date of its creation or the date when it last was in effect, whichever is later." The CLI's `--audit-retention-days` sets it. `test/integration/audit_retention_test.dart` (2: an event aged past a 30-day retention goes with its history and index rows and a 20-day one stays; the default is six years and a five-year-old event stays; the first fails with the sweep's cutoff neutralized). A16 closed. **Amended the same day: the default is no deletion.** The six years was my reading; NIST SP 800-66r2 §5.3.2 (Audit Controls, read whole) leaves what is recorded, reviewed and kept to "the regulated entity's risk assessment" and sets no retention period for audit records, and §164.316(b)(2)(i) covers "documentation required by paragraph (b)(1)", which neither source says includes audit-log entries. Retention now runs only when a deployment sets `--audit-retention-days`; the test for the default (a 20-year-old event stays) fails under the old six-year default.

---

## 4. Searching, SQL, and Dart against SQL

**Assessment.** The SQL is the strongest part of the codebase. Covering indexes per value column, keyset paging, ranges instead of LIKE, sentinel bounds instead of NULL, `+column` to steer the planner, partial indexes for contained rows: each is measured on the 929k MIMIC store and the measurement is written beside the code. Parameters are bound everywhere user text reaches SQL; the string-built SQL (`_historyCompartmentSql`, the PRAGMA key, table names in backup) takes only generated constants or escaped literals. I found no injection.

The structural weakness is that **search is implemented twice.** `_pagedIds` (SQL) handles nearly everything; `_matchingIds` and the `_search*Parameter` functions (`fhir_dao.dart:2900-3066, 3708-5447`, about 1,600 lines) are an older Dart set-arithmetic engine still reached for `:of-type`, `_id` lists over 500, `_filter` sets over 500, and an unsortable `_sort`, and then it evaluates every other parameter of that request too. It reads whole rows including the JSON to collect ids (`:3054`, `:3771`, `:3926`, `:4009`, `:4569`, `:4714`), runs one search per reference row for a chain (`:5233-5250`), and ignores `referenceParam` in `_has` (`:5431-5437`). For the one bad value I tried the two paths agreed (both 400). Recommendation: make the general path compose the same `_conditionFor` conditions (one `SELECT id` per parameter, intersected), keep Dart only for the `:of-type` post-filter, and delete the per-type functions.

**Q3. 🟠 Modifiers the specification does not give a type are accepted and answered as something else. CONFIRMED.**

> ✅ **FIXED 2026-09-18** in fhir_db (`modifiersByType`: string loses `text`, reference loses the literal `type`) and in the R5 and R6 bindings, whose tables had the same two entries; there string `:text` is a defined modifier ("advanced text handling", from each version's modifier table read row by row) that the store does not implement, so it moves to the unsupported table and is refused as such. Two tests in each binding's `search_syntax_test.dart`. Both searches are now 400 through fhirant (`fix_q3/`).
R4B search.html 3.1.1.4.4 (section printed whole 2026-09-17, verbatim): "For string: :exact … or :contains", "For reference: :[type] where [type] is the name of a type of resource, :identifier, and, for some parameters, :above and :below", and "Server SHALL reject any search request that contains is suffixed by a modifier that the server does not support for that parameter". `modifiersByType` (`search_query_key.dart:250-258`) gives string `text` and reference a literal `type`. `Patient?name:text=tex` **200 total 1** (answered as starts-with); `Observation?subject:type=Patient` **200 total 0**.
**Q4. 🟡 `:missing=maybe` is read as false: 200. CONFIRMED.** Only `true` and `false` are values.

> ✅ **FIXED 2026-09-18** in fhir_db: `_missingValue` admits `true` and `false` (case-insensitive) on both search paths and throws `InvalidSearchValue` otherwise, which fhirant already answers as 400. Tests in the R4B binding and `unsupported_modifier_test.dart`. An EMPTY value (`gender:missing=`) is dropped with every other empty value before a modifier is read, and search.html says nothing about it; left as it was.
**Q5. 🟡 `subject:identifier=|42` ignores the no-system form: 2 matches, 1 expected. CONFIRMED.** The token rule for a leading pipe (`_tokenCondition`) is not applied to `:identifier`.

> ✅ **FIXED 2026-09-18** in fhir_db: one `_identifierCondition` (value under any system, `system|value`, and `|value` = identifier with no system) serves both the paged and the general path, which each had their own copy of the two-form version. Two tests in the R4B binding; the `|12345` test fails without the fix (`fix_q5/02_fails_without_fix.log`).
**Q6. 🟠 A Bundle GET entry ignores `_has` (and `_include`, `_summary`, `_elements`, `_filter`). CONFIRMED.** REST total 1, the same search as a batch entry total **2**. `_entrySearch` (`bundle_handler.dart:1013`) passes `searchParams`, `count`, `offset`, `sort` and nothing else.

> ✅ **FIXED 2026-09-18.** One search. The body of `_searchResources` is `typeSearch` (`resource_handler.dart`): explicit inputs (query, requested URI for links, `Prefer: handling`, principal, compartment), a Bundle out, `SearchRefused` for a 400 with the issue code the REST path always sent. The REST handler wraps it in a Response; `_entrySearch` wraps it in an entry, with the entry's URL under the base and the outer request's `Prefer` and principal. The lift was done by a script kept as the record (`fix_q6/lift_type_search.py`, every replacement asserting its count). `test/integration/bundle_entry_search_test.dart`: eight URLs (`_has`, `_include`, `_revinclude`, `_summary=count`, `_elements`, `_filter`, `_total=none`, plain) sent both ways must agree on total, entry ids, modes and resource shape; and `handling=strict` reaches the entry. Two mock-based unit tests needed the stubs the search now reads up front (`headers`, `requestedUri`). The first full run had one red elsewhere: `BackupService.isJsonFile` judged a file by its first byte, and an encrypted backup starts with a random salt, so one in 256 began with `{` and the restore read it as UTF-8 (ten reruns green, then reproduced deterministically). Fixed in the following commit: the first 64 bytes must be UTF-8 and open `{ "`. Server suite 1,206.
**Q7. 🟡 A Bundle entry URL with three segments is processed as its first two. CONFIRMED.** `GET Patient/a/_history` inside a batch returned **the Patient**. Also true of `Patient/a/$everything`.

> ✅ **FIXED 2026-09-18.** An entry URL with a third segment, or a second segment outside the id grammar (R4B datatypes.html `id`, verbatim: "Regex: [A-Za-z0-9\-\.]{1,64}"), is refused as unsupported with a 400 on that entry: this server processes `[type]` and `[type]/[id]`, either with a query, inside a Bundle. `test/integration/bundle_entry_url_test.dart`: five such URLs refused beside a read that succeeds; fails without the fix (`fix_q7/02_fails_without_fix.log`). Server suite 1,207.
**Q8. 🟡 `_contained=true|both` is refused as "this server does not index contained resources"** (`resource_handler.dart:485-490`, `search_parser.dart:113-118`, dated 2026-09-02). Contained resources have been indexed under `#Type` since fhir_db schema 7. BY READING.

> ✅ **FIXED 2026-09-19.** fhir_db gains `searchContainedIds` (the `#Type` rows' composite ids, `[containerType]/[containerId]#[containedId]`); `typeSearch` answers `_contained=true` and `both` per R4B 3.1.1.5.5 (read whole): the page is cut, in id order, from the contained set or the normal set followed by it; a contained match is the container (once) or, with `_containedType=contained`, the contained resource itself with fullUrl `[base]/[containerType]/[containerId]#[containedId]`; every entry `search.mode=match`. `_sort`, `_filter` and the includes are refused together with `_contained` (they would have to be re-done over the combined set) rather than answered as if applied. `test/integration/contained_search_test.dart`, the page's own MedicationRequest example: six tests, five fail without the change. Server suite 1,233. **Reverted the same day (Grey's ruling):** nothing we build sends `_contained`, and R4B 3.1.1.5.5 defines the parameter without requiring it. `true` and `both` are refused again, now with a true message ("is not supported by this server"; the old one said contained resources were not indexed, and they are). fhir_db's `searchContainedIds` is reverted with it. To be built later.
**Q9. 🟡 `If-None-Exist` searches have no `count`** (REST `resource_handler.dart:1203`, Bundle `:622`): every match is hydrated to learn whether there are 0, 1 or 2. `_typeFilter` in `$export` calls `search` where `searchIds` exists (`export_handler.dart:629, 723`). BY READING.

> ✅ **FIXED 2026-09-19.** Both conditional-create searches ask for two rows (none creates, one is returned, two is the 412); the Bundle mock test pins `count: 2`. Both `_typeFilter` sites read ids through `searchIds` and let the export stream the rows. Server suite 1,227.

**SQLite configuration. MEASURED** (`store_probe.tsv` F4, `store_probe2.tsv` WAL).

> ✅ **WAL with FULL, 2026-09-18.** `applyStoreCipher` sets `PRAGMA main.journal_mode = WAL` and `PRAGMA main.synchronous = FULL` after the key, on every open; the CLI and the app both open through it. `main.` because the unqualified form "changes the journaling mode ... for all attached databases" (pragma.html, verbatim) and a backup is ATTACHed to the same connection: a test proves a backup written from a WAL store is a plain rollback-journal file with no `-wal` sidecar. Through the real store function: 7.9 ms per commit (`fix_wal/commit_cost.tsv`) against 23.8 before. NORMAL (0.5 ms, may lose the last commits on power loss) is Grey's call and is not set. STAT4 still not looked at. Server suite 1,198, db 129, app 34.
The build: 3.53.4, `TEMP_STORE=2` (sort spills stay in memory, which is what an encrypted store needs), no `STAT4`. A file store opens with `journal_mode=delete synchronous=2 foreign_keys=0 busy_timeout=0`; nothing sets a PRAGMA but the cipher. 200 single-resource commits on this disk:

| journal_mode / synchronous | per commit |
|---|---|
| delete / FULL (today) | **23.8 ms** |
| wal / FULL | 8.5 ms |
| wal / NORMAL | 0.5 ms |

WAL with FULL keeps today's durability at about a third of the cost. WAL with NORMAL can lose the last commits on power loss (never corrupts); that choice is a clinical one. The probe-size planning in `_pagedIds` exists because the build has no STAT4; whether the `sqlite3` build hook can define `SQLITE_ENABLE_STAT4` is unknown, I did not look.

---

## 5. Conformance

**C1. 🟠 `$validate` answers 400 for an invalid resource. CONFIRMED.** OperationDefinition `Resource-validate`, `comment` (bundled `profiles-resources.ndjson`, verbatim): "This operation returns a 200 OK whether or not the resource is valid. A 4xx or 5xx error means that the validation itself could not be performed, and it is unknown whether the resource is valid or not." `validate_handler.dart:154`.

> ✅ **FIXED 2026-09-18.** An invalid resource is a 200 whose OperationOutcome carries the errors. The one "cannot be performed" case the engine reports as an issue rather than throwing, a base type it cannot resolve ("No StructureDefinition found for resourceType: X"), is a 422 `not-supported`, as the unresolvable-canonical case already was. New test with the packaged definitions loaded (`gender: not-a-gender` → 200 with an error issue); fails without the fix (`fix_c1/02_fails_without_fix.log`). Server suite 1,208.
**C2. 🟡 The CapabilityStatement claims `conditionalUpdate: true`; `PUT /Patient?identifier=` is 404. CONFIRMED.** There is no such route. (The code calls If-Match a conditional update; that is `versioning`.)

> ✅ **FIXED 2026-09-18.** `PUT /[type]?[search parameters]` exists, over REST and as a Bundle entry, by the five rows of R4B http.html 3.1.0.4.3 (read whole): no match creates (a server id, or the body's as update-as-create); one match updates it, or is a 400 when the body names a different id; several are a 412 `multiple-matches`; no criteria is a 400. `resolveConditionalUpdate` decides the target inside the caller's compartment; the write is then the ordinary `PUT /[type]/[id]` with all its rules. Ten tests through the pipeline (`test/integration/conditional_update_test.dart`), including a transaction and a patient-scoped token. The claim is true now; `conditionalCreate` (If-None-Exist) and `conditionalDelete` already were.
**C3. 🟡 It cites OperationDefinitions that do not exist. CONFIRMED** against the bundled specification: `Practitioner-everything`, `RelatedPerson-everything`, `Device-everything` (`metadata_handler.dart:275-281`); R4B has `Patient-`, `Encounter-`, `Group-` and `MedicinalProductDefinition-everything`.

> ✅ **FIXED 2026-09-18.** Every definition the CapabilityStatement cites is now held by the server after the specification load, and a test proves it for all of them (`test/handlers/metadata_definitions_test.dart`, over the shipped files). That test found two more than the review: the Bulk Data `export` canonical (an IG, not in R4B) and this server's own `$fhirpath`, cited under a host that published nothing. Shipped in `assets/fhir_spec/`: `bulkdata-operations.ndjson`, HL7's published `OperationDefinition/export` (v3.0.0, narrative removed), and `fhirant-operations.ndjson`, this server's `Practitioner-`, `RelatedPerson-` and `Device-everything` (modelled on HL7's `Patient-everything`, with the parameters the handler honours) and `fhirpath`, all under `http://fhirfli.dev/fhirant/OperationDefinition/`. Patient and Encounter keep HL7's canonicals.
**C4. 🟡 `oauth-uris` still advertises `register` → `/auth/register`**, which creates user accounts (removed from `.well-known` on 2026-09-08 for that reason). BY READING.

> ✅ **FIXED 2026-09-18.** The extension names `authorize` and `token` only; a test pins that set. Server suite 1,221.
**C5. 🟡 PUT accepts an id outside the FHIR id grammar: `PUT /Patient/a$b_c` 201. CONFIRMED.**

> ✅ **FIXED 2026-09-18.** `utils/fhir_id.dart` holds the grammar (R4B datatypes.html `id`, verbatim: "Regex: [A-Za-z0-9\-\.]{1,64}"); `PUT /[type]/[id]` refuses an id outside it with a 400, and the Bundle entry check of Q7 uses the same function. `test/integration/put_id_grammar_test.dart`: four bad ids refused and not stored, four good ones created. Server suite 1,223.
**C6. 🟡 The 406 body interpolates the Accept header into JSON unescaped: invalid JSON. CONFIRMED.** `reindex_handler.dart:153-161` escapes JSON by hand too.

> ✅ **FIXED 2026-09-18.** Both 406 bodies and the reindex handler's OperationOutcome go through the JSON encoder. A test sends `Accept: text/plain; q="1\"2"` and parses the 406 body; it fails without the fix (`fix_c6/02_fails_without_fix.log`). Server suite 1,224.
**C7. 🟡 `$translate` returns the first target only.** BY READING.

> ✅ **FIXED 2026-09-18.** Every target of every element for the code, across the groups whose source matches, is a `match` part with `equivalence`, `concept`, `product` and `source`; `result` is true only when a match's equivalence is not `unmatched` or `disjoint` (OperationDefinition ConceptMap-translate, bundled, verbatim in the code). `test/integration/translate_test.dart`: three targets across two groups, an unmatched target, an unknown code; two of three fail without the fix. Server suite 1,227.

---

## 6. Setup

**S1. 🟠 `docker build` fails from a working checkout. CONFIRMED** (`docker_smoke.log`, exit 1).

> ✅ **FIXED 2026-09-18.** `.dockerignore`: pub state and overrides, build output, coverage, IDE files, `.git`, runtime data, `.pem`, the two packages the Dockerfile never copies, and `tool/review_*`. The context was 11 GB measured (9.3 GB the app's build output). From this checkout the image now builds (`fix_s1/01_docker_build.log`) and `test_docker.sh` passes 28 of 28 (`fix_s1/02_smoke.log`); `pub get` in the image re-resolves from the pubspec's git overrides, the tracked lock's `/home/grey` paths do not stop it. CI gains a `docker` job that runs `test_docker.sh` on every push, so the image is built by someone. No `.dockerignore`: the gitignored `pubspec_overrides.yaml` (absolute paths under `/home/grey/dev/fhir/`) and the 248 MB `.dart_tool` are copied in, and `pub get` fails on `/home/grey/dev/fhir/fhirpath`. A clean clone builds. CI never builds the image, so `test_docker.sh` is run by nobody.
**S2. 🟠 An exception in the hourly cleanup kills the CLI. CONFIRMED** (`timer_error.log`: exit 255).

> ✅ **FIXED 2026-09-18.** The sweep is `FhirAntServer.hourlyCleanup`: each of the five steps runs in its own try/catch and is logged, the later steps still run, and the timer callback only calls it. Two tests over a mock store whose steps throw (`test/services/hourly_cleanup_test.dart`); with the guard removed both fail (`fix_s2/02_fails_when_unguarded.log`). Server suite 1,197. `fhirant_server.dart:772-788`: `cleanupRevokedTokens`, `cleanupAuthorizationCodes` and `optimizeStatistics` are awaited in a `Timer.periodic` callback with no try/catch. One "database is locked" ends the process. (The Flutter app's zone survives it.)
**S4. 🟡 The CLI's authentication default is the opposite of the app's, and its off switch does two things.** The app defaults to authentication off (`kDefaultAuthDisabled`). The CLI and the Docker image default to on; the only way off is `--dev-mode`, which also accepts the public default encryption key (`bin/server.dart:94-107`). `CLAUDE.md` documents "Run the server (dev mode — no auth required)" with a command that has no `--dev-mode` and would refuse to start. One setting for authentication, the same default in both, independent of the key check. BY READING.

> ✅ **FIXED 2026-09-19 (the independence).** `--dev-mode` switches authentication off and nothing else; opening a store under a key this repository publishes takes its own flag, `--allow-public-key` (`encryptionKeyRefusal` in `lib/src/cli/options.dart`, also a config-file key). Through the real binary with no `FHIRANT_ENCRYPTION_KEY` (`tool/review_2026-09-17/fix_s4/`): the old `--dev-mode` opened the store under the public key and served without authentication (`GET /Patient` 200); the new `--dev-mode` refuses to start (exit 1), and `--allow-public-key` alone serves with authentication on (401, bootstrap token logged). `test/cli/options_test.dart` +4. `CLAUDE.md`'s run commands corrected. The different DEFAULTS of the app and the CLI are F5's settled decision (the app flips at deployment) and are not changed here.
**S5. 🟡 The Dockerfile HEALTHCHECK runs `server --help`** (it starts a second binary and asks nothing of the running one), and `CMD [...] || exit 1` after a JSON array makes Docker read it in shell form. Not run: S1 blocks the build here. `/health` exists.

> ✅ **FIXED 2026-09-19.** Measured first: with the old line every container Docker ran from the image went **unhealthy** (`tool/review_2026-09-17/fix_s5/02_docker_old_healthcheck.log`), so the check was not merely uninformative, it failed. Now `HEALTHCHECK ... CMD curl -fsS http://localhost:8080/health || exit 1`, the shape the Dockerfile reference gives (buildkit `frontend/dockerfile/docs/reference.md`, HEALTHCHECK, read whole, verbatim): "0: success - the container is healthy and ready for use", "1: unhealthy - the container isn't working correctly", "2: reserved - don't use this exit code", example `CMD curl -f http://localhost/ || exit 1`. `/health` is public; the runtime image has curl. `test_docker.sh` waits for Docker's own verdict and checks the probe read `/health`: 37/37 (`01_docker_new.log`); against the old line the same two assertions fail.
**S6. 🟡 `--config` is declared and never read; `http: any`; `int.parse(port)` is unguarded.** BY READING.

> ✅ **FIXED 2026-09-19.** The CLI's settings resolve through `lib/src/cli/options.dart` (`ServerOptions`): the command line over the `--config` YAML file over the defaults. The file's keys are the long option names; an unknown key, a non-map file or unparsable YAML is a usage error, so a misspelling is not silently ignored. Port must be an integer 1–65535 and `--audit-retention-days` 1–36500, else a usage line and exit 1. `http: any` pinned to `^1.6.0`, the version the lock resolves. `test/cli/options_test.dart` (8). Through the real binary (`tool/review_2026-09-17/fix_s6/`): `--port abc` was `FormatException: Invalid radix-10 number`, exit 255, and is now "port must be an integer from 1 to 65535", exit 1; a config file with `prot: 9090` was ignored and the run went on to the encryption-key check, and is now "unknown option "prot"", exit 1.
**S8. 🟡 The tracked `pubspec.lock` files record this machine's gitignored overrides. CONFIRMED** (`grep -c /home/grey` per tracked lock, 2026-09-17: fhirant 4, fhirant_db 4, fhirant_server 5, the other three 0). `packages/fhirant_server/pubspec.lock` names `path: "/home/grey/dev/fhir/fhir_db"` and `/home/grey/dev/fhir/fhirpath`. A clone cannot honour those entries, so `pub get` re-resolves there, and the comments in `Dockerfile` and `pubspec.yaml` that say cicada is fetched "at the commit the lock file names" describe a lock no other machine uses. Either stop tracking the locks of packages developed against local overrides, or generate them without the overrides before committing.

> ✅ **FIXED 2026-09-19.** Measured first: a clean build (the S5 Docker run) resolved the five overridden packages to whatever their branch held that minute (`fhir_db` at `de0d3b`, `fhir_r4` at `1e7ddb`, the commits pushed minutes earlier), so the image was not reproducible. The lock cannot pin them here: pub writes a local override into it by design (dart-lang/site-www `tools/pub/dependencies.md`, verbatim: "the pubspec's lockfile is updated to reflect the new path to your dependency"). So the pin moved to where it holds on every machine: each git `dependency_override` in `fhirant`, `fhirant_server` and `fhirant_db` names a commit, not `main`/`dev`. `cicada` was already pinned by the lock's git `resolved-ref`, and the clean build kept it (not listed as changed), so the Dockerfile's comment about it is true. The library locks and the root aggregate's are no longer tracked, per `tools/pub/private-files.md` (verbatim): "For regular packages, don't commit the pubspec.lock file." The two applications keep theirs ("For application packages, we recommend that you commit the pubspec.lock file."); their overridden entries still name this machine's paths, which a clone ignores in favour of the pins. The app's lock was also stale (`fhir_path` 0.14.1 from pub.dev, `fhir_db` 0.13.0) and now matches its pubspec. Suites server 1,307, db 129, app 34; Docker 37/37 (`tool/review_2026-09-17/fix_s8/`). A pin moves on purpose, in its own commit, until the family release removes the overrides.
**S7. 🟡 `CLAUDE.md` is stale:** `--sqlcipher-path`, `--dev`, "pub.dev ^0.7.0", "flutter_lints", "Published on Google Play", a `logs` table (dropped at schema 22), "Version ID = timestamp" (`versionIdAsTime = false`), `melos.yaml`. BY READING.

> ✅ **FIXED 2026-09-19.** Each line checked against the code, then corrected in `CLAUDE.md`: no `--sqlcipher-path` flag (removed; `--spec-path`, `--base-url` listed); the family packages at ^0.12.0 with the git `dependency_overrides` named, not "pub.dev ^0.7.0"; `very_good_analysis` in every package, not `flutter_lints`; the app "Not published", per the release record; no `logs` table (dropped at schema 22); version ids a counter (`versionIdAsTime = false`); no `melos.yaml`. Also stale and fixed: `flutter test` for the two pure Dart packages (now `dart test`); a "dev mode" run command without `--dev-mode` (now both runs, with the key and first-administrator requirements); a `build_runner` step for a package with no code generation; "Tests use `flutter_test`" (server and db use `package:test`).

---

## 7. Structure and coding

**Assessment.** The package split is right (app → server → db → logging; secure storage beside it), the store is model-independent with a thin binding, handlers are functions over a `FhirAntDb`, and authorization was pulled into one `Principal` on 2026-09-09. Comments record the measurement or the specification sentence behind a choice, which is why this review could check them. What follows is what the structure still costs.

- **ST1. Response helpers are copied into every handler.** 22 private `_errorResponse` / `_validationErrorResponse` / `_operationOutcome` / `_outcome` / `_baseUrl` functions and 28 hand-built OperationOutcome maps across `handlers/`, beside a shared `operation_outcomes.dart` that has two functions. They already disagree: some answer `application/json`, some `application/fhir+json`, some no content type; some build the FHIR class and some a Map.
- **ST2. Authorization is still spread across three layers.** The middleware checks the first path segment; each handler then re-checks the type it really returns (`compartment_handler`, `export_handler`, `document_handler`, `cql_handler`, system search, history), each in its own words. A5, A6 and A7 are all "one path forgot". A route table that declares, per route, the permission, the type it returns and whether it is compartment-confined would make the omission impossible to write.
- **ST3. `parseQueryParameters` returns `Map<String, dynamic>`** read back with fourteen casts at each of five call sites. A class.
- **ST4. Two search engines** (section 4).
- **ST5. `fhirant_secure_storage` is half dead.** `getEncryptionKey`, `saveEncryptionKey`, `storePasskey`, `getPasskey`, `generateRandomKey`, `generateSecureRandomKey` have no caller; the app reads and writes its keys with `FlutterSecureStorage` directly under different key names (`database_service.dart:9`, `server_service.dart:6`). `fhir_db`'s `cipher_from_key.dart` (a third PBKDF2) is also unused by fhirant.
- **ST6. Three hand-written PBKDF2 loops** (`password_hasher.dart`, `backup_crypto.dart`, `fhir_db/cipher_from_key.dart`).
- **ST7. Errors are swallowed at the store boundary.** `FhirAntDb.saveResource` catches everything, writes `$e` to stderr and returns null (`fhirant_db.dart:709-712`); `FhirDao.saveResources` prints and returns false; the spec loader and the audit queue ignore that false. The handler then answers "Database operation failed" with the cause gone.
- **ST8. Blocking work on the server isolate:** password hashing, the rebuild (CLI), client programs, the CapabilityStatement rebuilt on every `GET /metadata` (146 types; public).

---

## 8. Against the three earlier reviews

Opened only after sections 2-7 were written.

| | |
|---|---|
| Not in any earlier review | R1, F1, S3, Q1, Q2, T1, A1, A2, A3, A4, A5, A6, A9, A10, Q3-Q7, C1-C3, C5, C6, S1, S2, the SQLite configuration, ST1-ST8 |
| Dismissed earlier by reading, confirmed now by probe | T2 (REVIEW-2026-09-08 line 30) |
| A consequence of an earlier fix | A3: 2026-09-08 row 14 made the middleware refuse a locked account's tokens; that is what lets a stranger end a session. A1: row 3's per-type confinement fails open when the token has no patient. S3: §6.1's tag. |
| Earlier fix that did not reach every path | A6 (SearchParameter authority, 2026-09-14), A7 (row 18 covered PATCH for rest-hook authority, not for activation or notification), T1 (finding 24), C3/C4 (row 31, row 20) |
| Earlier fix weaker than the problem | A4: 2026-09-06 finding 10 recommended "at least pin `redirect_uri` per `client_id` on first use", and that is what was built. The probe shows first use is the attack. |
| Raised before and still as it was | A8: SECURITY-REVIEW-2026-08-25 F4 fixed `$backup`'s plaintext; `$export` was not considered. A11: the 120,000 figure is recorded as a strength in SECURITY-REVIEW line 529. |

Why three reviews missed R1, Q1 and S3: each needs a condition the tests never create (the production cipher pragmas; a second time zone; a client writing a server's tag). The earlier reviews probed requests against the test harness's store. This one also asked, per mechanism, what production does that the harness does not.

---

## 9. Order of work

1. R1, with the production-configured backup test first so it fails.
2. F1, S3, T1, T2: wrong answers and lost records, each small.
3. Q1 and Q2 together (both end in an index rebuild): UTC for zone-less dates, shadow-table rebuild.
4. A1, A2, A3, A5, A6, A7 as one change: the route table of ST2, with AuditEvent append-only and HEAD mapped to the GET permission.
5. A4 (client registration), A8 (stream the export), A9/A11 (worker isolate), A10 (body limit).
6. S1, S2, then WAL with `synchronous=FULL`.
7. Q3-Q9, C1-C7, then ST1, ST3-ST7; the general search path last, behind a differential test that runs every search both ways.

---

## Appendix A. Files read whole (119)

| read | lines | file |
|---|---|---|
| ☑ | 3 | fhirant/packages/fhirant_db/lib/fhirant_db.dart |
| ☑ | 4 | fhirant/packages/fhirant_server/lib/src/middlewares/middlewares.dart |
| ☑ | 8 | fhirant/packages/fhirant_server/lib/src/utils/utils.dart |
| ☑ | 9 | fhirant/packages/fhirant_server/lib/src/handlers/base_handler.dart |
| ☑ | 12 | fhirant/packages/fhirant_server/lib/src/utils/token_hasher.dart |
| ☑ | 13 | fhirant/packages/fhirant_server/lib/src/handlers/favico_handler.dart |
| ☑ | 17 | fhirant/packages/fhirant_server/lib/fhirant_server.dart |
| ☑ | 20 | fhirant/packages/fhirant/lib/src/config/security_config.dart |
| ☑ | 26 | fhirant/packages/fhirant_server/lib/src/handlers/auth_status_handler.dart |
| ☑ | 28 | fhirant/packages/fhirant_server/lib/src/handlers/handlers.dart |
| ☑ | 35 | fhirant/packages/fhirant_server/lib/src/handlers/health_handler.dart |
| ☑ | 38 | fhirant/packages/fhirant_server/lib/src/handlers/websocket_handler.dart |
| ☑ | 39 | fhirant/packages/fhirant_server/lib/src/utils/pkce.dart |
| ☑ | 40 | fhirant/packages/fhirant_server/lib/src/utils/search_param_definitions.dart |
| ☑ | 41 | fhirant/packages/fhirant_server/lib/src/utils/operation_outcomes.dart |
| ☑ | 49 | fhirant/packages/fhirant/lib/src/screens/dashboard_screen.dart |
| ☑ | 53 | fhirant/packages/fhirant_server/lib/src/utils/password_policy.dart |
| ☑ | 64 | fhirant/packages/fhirant_server/lib/src/handlers/smart_config_handler.dart |
| ☑ | 70 | fhirant/packages/fhirant_server/lib/src/middlewares/content_negotiation.dart |
| ☑ | 74 | fhirant/packages/fhirant_server/lib/src/utils/jwt_secret.dart |
| ☑ | 75 | fhirant/packages/fhirant/lib/src/services/server_service.dart |
| ☑ | 87 | fhirant/packages/fhirant_server/lib/src/services/websocket_subscriptions.dart |
| ☑ | 91 | fhirant/packages/fhirant/lib/src/services/database_service.dart |
| ☑ | 92 | fhirant/packages/fhirant_server/lib/src/middlewares/cors_middleware.dart |
| ☑ | 92 | fhirant/packages/fhirant_server/lib/src/utils/http_headers.dart |
| ☑ | 102 | fhirant/packages/fhirant_server/lib/src/auth/admin_provisioning.dart |
| ☑ | 115 | fhirant/packages/fhirant_server/lib/src/utils/patient_scope.dart |
| ☑ | 117 | fhirant/packages/fhirant_server/lib/src/auth/credential_check.dart |
| ☑ | 121 | fhirant/packages/fhirant_server/lib/src/utils/jwt_service.dart |
| ☑ | 131 | fhirant/packages/fhirant_server/lib/src/utils/password_hasher.dart |
| ☑ | 133 | fhirant/packages/fhirant_server/lib/src/utils/response_shaper.dart |
| ☑ | 134 | fhirant/packages/fhirant/lib/src/widgets/network_info_card.dart |
| ☑ | 136 | fhirant/packages/fhirant_server/lib/src/utils/db_resource_cache.dart |
| ☑ | 145 | fhirant/packages/fhirant_server/lib/src/handlers/revoke_handler.dart |
| ☑ | 150 | fhirant/packages/fhirant_server/lib/src/handlers/login_handler.dart |
| ☑ | 155 | fhirant/packages/fhirant_logging/lib/fhirant_logging.dart |
| ☑ | 155 | fhirant/packages/fhirant_server/lib/src/middlewares/auth_middleware.dart |
| ☑ | 161 | fhirant/packages/fhirant_server/lib/src/handlers/reindex_handler.dart |
| ☑ | 163 | fhirant/packages/fhirant/lib/src/widgets/admin_setup_dialog.dart |
| ☑ | 165 | fhirant/packages/fhirant_db/lib/db/server_types.dart |
| ☑ | 168 | fhirant/packages/fhirant/lib/src/widgets/request_log_card.dart |
| ☑ | 176 | fhirant/packages/fhirant_server/lib/src/utils/search_links.dart |
| ☑ | 191 | fhirant/packages/fhirant_server/lib/src/handlers/fhirpath_handler.dart |
| ☑ | 196 | fhirant/packages/fhirant/lib/src/widgets/resource_counts_card.dart |
| ☑ | 208 | fhirant/packages/fhirant_server/lib/src/handlers/register_handler.dart |
| ☑ | 215 | fhirant/packages/fhirant_server/bin/server.dart |
| ☑ | 218 | fhirant/packages/fhirant_server/lib/src/utils/backup_crypto.dart |
| ☑ | 219 | fhirant/packages/fhirant_server/lib/src/handlers/validate_handler.dart |
| ☑ | 224 | fhirant/packages/fhirant_server/lib/src/utils/filter_evaluator.dart |
| ☑ | 238 | fhirant/packages/fhirant/lib/src/widgets/server_control_card.dart |
| ☑ | 242 | fhirant/packages/fhirant_server/lib/src/auth/request_authorization.dart |
| ☑ | 246 | fhirant/packages/fhirant_server/lib/src/utils/spec_loader.dart |
| ☑ | 251 | fhirant/packages/fhirant_server/lib/src/handlers/backup_handler.dart |
| ☑ | 260 | fhirant/packages/fhirant_server/lib/src/handlers/patch_handler.dart |
| ☑ | 261 | fhirant/packages/fhirant/lib/src/state/server_state.dart |
| ☑ | 264 | fhirant/packages/fhirant_server/lib/src/services/backup_service.dart |
| ☑ | 273 | fhirant/packages/fhirant_server/lib/src/handlers/forecast_handler.dart |
| ☑ | 283 | fhirant/packages/fhirant_secure_storage/lib/fhirant_secure_storage.dart |
| ☑ | 289 | fhirant/packages/fhirant_server/lib/src/handlers/document_handler.dart |
| ☑ | 290 | fhirant/packages/fhirant/lib/main.dart |
| ☑ | 314 | fhirant/packages/fhirant_server/lib/src/utils/search_parser.dart |
| ☑ | 324 | fhirant/packages/fhirant/lib/src/widgets/backup_card.dart |
| ☑ | 335 | fhirant/packages/fhirant_server/lib/src/utils/json_patch.dart |
| ☑ | 380 | fhirant/packages/fhirant/lib/src/screens/onboarding_screen.dart |
| ☑ | 382 | fhirant/packages/fhirant_server/lib/src/handlers/meta_handler.dart |
| ☑ | 396 | fhirant/packages/fhirant_server/lib/src/handlers/refresh_handler.dart |
| ☑ | 400 | fhirant/packages/fhirant_server/lib/src/utils/smart_scopes.dart |
| ☑ | 421 | fhirant/packages/fhirant_server/lib/src/handlers/mapping_handler.dart |
| ☑ | 427 | fhirant/packages/fhirant_server/lib/src/handlers/metadata_handler.dart |
| ☑ | 446 | fhirant/packages/fhirant_server/lib/src/middlewares/audit_middleware.dart |
| ☑ | 454 | fhirant/packages/fhirant_server/lib/src/utils/filter_expression.dart |
| ☑ | 499 | fhirant/packages/fhirant_server/lib/src/services/subscription_service.dart |
| ☑ | 536 | fhirant/packages/fhirant_server/lib/src/handlers/history_handler.dart |
| ☑ | 537 | fhirant/packages/fhirant_server/lib/src/handlers/compartment_handler.dart |
| ☑ | 600 | fhirant/packages/fhirant/lib/src/screens/resource_browser_screen.dart |
| ☑ | 662 | fhirant/packages/fhirant_server/lib/src/handlers/authorize_handler.dart |
| ☑ | 730 | fhirant/packages/fhirant_server/lib/src/handlers/cql_handler.dart |
| ☑ | 895 | fhirant/packages/fhirant_server/lib/src/handlers/export_handler.dart |
| ☑ | 973 | fhirant/packages/fhirant_server/lib/src/fhirant_server.dart |
| ☑ | 1234 | fhirant/packages/fhirant_server/lib/src/handlers/terminology_handler.dart |
| ☑ | 1244 | fhirant/packages/fhirant_server/lib/src/handlers/bundle_handler.dart |
| ☑ | 1716 | fhirant/packages/fhirant_db/lib/db/fhirant_db.dart |
| ☑ | 1930 | fhirant/packages/fhirant_server/lib/src/handlers/resource_handler.dart |
| ☑ | 24 | fhir_db/lib/fhir_db.dart |
| ☑ | 110 | fhir_db/lib/src/cipher_from_key.dart |
| ☑ | 5746 | fhir_db/lib/src/fhir_dao.dart |
| ☑ | 754 | fhir_db/lib/src/fhir_db.dart |
| ☑ | 116 | fhir_db/lib/src/fhir_model.dart |
| ☑ | 89 | fhir_db/lib/src/has_parameter.dart |
| ☑ | 14 | fhir_db/lib/src/new_id.dart |
| ☑ | 25 | fhir_db/lib/src/search/compartment_scope.dart |
| ☑ | 102 | fhir_db/lib/src/search/contained_index.dart |
| ☑ | 474 | fhir_db/lib/src/search/custom_search_parameters.dart |
| ☑ | 148 | fhir_db/lib/src/search/fhir_date.dart |
| ☑ | 46 | fhir_db/lib/src/search/implicit_range.dart |
| ☑ | 139 | fhir_db/lib/src/search/normalize.dart |
| ☑ | 47 | fhir_db/lib/src/search/search_date_range.dart |
| ☑ | 83 | fhir_db/lib/src/search/search_escaping.dart |
| ☑ | 1074 | fhir_db/lib/src/search/search_indexer.dart |
| ☑ | 137 | fhir_db/lib/src/search/search_parameter_types.dart |
| ☑ | 337 | fhir_db/lib/src/search/search_query_key.dart |
| ☑ | 17 | fhir_db/lib/src/tables/canonical_table.dart |
| ☑ | 95 | fhir_db/lib/src/tables/composite_search_table.dart |
| ☑ | 54 | fhir_db/lib/src/tables/date_search_table.dart |
| ☑ | 13 | fhir_db/lib/src/tables/general_storage_table.dart |
| ☑ | 43 | fhir_db/lib/src/tables/number_search_table.dart |
| ☑ | 54 | fhir_db/lib/src/tables/quantity_search_table.dart |
| ☑ | 52 | fhir_db/lib/src/tables/reference_search_table.dart |
| ☑ | 55 | fhir_db/lib/src/tables/resource_table.dart |
| ☑ | 39 | fhir_db/lib/src/tables/special_search_table.dart |
| ☑ | 60 | fhir_db/lib/src/tables/string_search_table.dart |
| ☑ | 23 | fhir_db/lib/src/tables/sync_table.dart |
| ☑ | 13 | fhir_db/lib/src/tables/tables.dart |
| ☑ | 42 | fhir_db/lib/src/tables/token_search_table.dart |
| ☑ | 33 | fhir_db/lib/src/tables/uri_search_table.dart |
| ☑ | 14 | fhir_r4_db/lib/fhir_r4_db.dart |
| ☑ | 38 | fhir_r4_db/lib/src/fhir_db.dart |
| ☑ | 87 | fhir_r4_db/lib/src/r4_model.dart |
| ☑ | 241 | fhir_r4_db/lib/src/search/compartment_definitions.dart |

**CLOSED 2026-09-08.** Every candidate below is resolved in ../../../../REVIEW-2026-09-08.md: 36 confirmed by probe (probes/OUTPUT.txt), C8/C33/C3/C45/C42/C47 refuted or void, the rest BY READING. Nothing in this file is open; it is the reading log.

# Review 2026-09-08 — working notes (written as files are read; the report is ../../../../REVIEW-2026-09-08.md)

Scope: every hand-written Dart file in fhirant (5 packages), fhir_r4_db (FhirDb, FhirDao, tables, search
helpers, cipher), fhir_r4_bulk, bin, tool, Docker, compose, CI, docs. Generated files (search_parameters.dart,
search_parameter_types.dart, *.g.dart) checked structurally, not read line by line.

Still open FROM the 2026-09-06 review (not re-derived here): finding 15 per-entry Bundle audit; §4.5 history
duplicates the current version (17% of the file); §4.6 three DAO copies; subscription cache; export isolate;
CLI same-isolate connection (unmeasured).

## Reading log (file → notes; "ok" = nothing found)
- bin/server.dart: ok. Notes: binds anyIPv4 only (no IPv6, not raised); SIGINT/SIGTERM both call stop+close, a second signal re-enters.
- fhirant_server.dart: candidates —
  C1 `/admin/unlock/<userId>`: check authMiddleware gates /admin on role admin (route comment says "protected by auth middleware").
  C2 RequestLogEntry.path is the UNREDACTED requestedUri.path (the log LINE is redacted); it feeds the app's request-log card. On-device only; low.
  C3 `_onPaths` keys the credential limiter on exact `request.url.path`; check `/auth/login/`, `//auth/login`, `/auth/./login` reach the handler without the tight bucket.
  C4 No top-level error middleware: an exception escaping a handler is shelf's plain-text 500, not an OperationOutcome, and the request log line is skipped (the logging middleware does not catch). Probe with a handler that throws.
  C5 stop(): audit drained BEFORE `_server.close(force: true)`; a request completing during the close is audited into a queue nobody drains. Low.
  C6 Rate limiters use MemStorage keyed per address, never pruned by us (library detail; check shelf_rate_limiter's MemStorage eviction).
- auth_middleware.dart: candidates —
  C7 🔴 `isPublic = path.isEmpty` covers `POST /` (transaction/batch Bundle) and `GET /?params` (system search): both reach their handlers with NO credential. PROBE: auth on, no token, POST / transaction creating a Patient; GET /?_id=x.
  C8 `POST /_search` and `GET /_history` name no resource type → resourceTypeFromPath null → no scope check unless isRootDataOperation lists them. Check smart_scopes; PROBE with a patient-scoped or readonly-narrow token.
  C1' `POST /admin/unlock/<id>`: 'admin' is lowercase → resourceTypeFromPath null → unless the handler checks role, any authenticated user unlocks any account. Check handler + PROBE.
- audit_middleware.dart: candidates —
  C9 `_entityReference` builds `what.reference` from ANY two-segment path: `auth/login`, `admin/unlock`, `Patient/$export`, `ValueSet/$expand` become references, and `subjectOfCare('auth','login')` is queried. Wrong record, low.
  C10 `_mapAction`: POST `/Type/_search` and POST `$operations` are recorded action C "create"; a POST search is a read (R). Wrong trail entry, low.
  C11 401/403 OperationOutcome bodies from the middleware carry no content-type (Response(401, body: jsonEncode)) → served as text/plain? content_negotiation only rewrites when the header CONTAINS application/json. PROBE with curl -i.
  ok: AuditQueue single writer; bare 401 not audited; audit never blocks.
- cors_middleware.dart: ok. content_negotiation.dart: ok apart from C11.
- jwt_service.dart: ok (alg pinned, refresh refused as access). jwt_secret.dart: ok (write then chmod: umask window, negligible).
- credential_check.dart: ok (inactive returns before the hash: timing tells "inactive" from "unknown"; negligible).
- admin_provisioning.dart: ok (count-then-create race; on-device, negligible). password_hasher.dart: ok.
- password_policy, pkce, token_hasher: ok.
- smart_scopes.dart / patient_scope.dart: candidates —
  C12 🔴 Mixed-context tokens defeat the patient filter: `extractPatientContext` returns null unless EVERY scope is patient/ (`isPatientOnlyContext`), while `isAuthorized` grants Observation read from `patient/Observation.rs`. A token `patient/Observation.rs user/Practitioner.rs` (an ordinary SMART patient app) reads every Observation. PROBE.
  C13 `Patient/$export` and `Group/<id>/$export` are governed by the type check on the FIRST segment ('r' on Patient / Group) but `$export-poll-status` and `$export-file` are admin-only: a non-admin kicks off a job it can never poll. And `user/Group.r` alone starts an export of every compartment type. PROBE both.
  C14 `Composition/<id>/$document` is 'r' on Composition and returns Patient/Practitioner/… ; same class as finding 4. Check handler; PROBE with `user/Composition.rs`.
  C15 `GET /_history` and `POST /_search`: resourceTypeFromPath returns '_history'/'_search' as the type, so wildcard scopes pass (incl. patient/*.rs with a context); whether the handlers apply the compartment is for history_handler / resource_handler. Check.
  C1'' `admin/unlock`: 'admin' is the "type", POST → 'c' → clinician `user/*.*` passes the middleware; handler must check role.
  PS: a patient-only token may POST a new Patient with no id (creates someone else's record). Low. Non-compartment types (Practitioner…) are unreadable to patient-only tokens — a choice, safe, not raised.
- http_headers.dart: ok.
- bundle_handler.dart: candidates —
  C24 🔴 No authorization inside a Bundle: `_processBundleEntry` checks neither scope nor patient compartment, and the middleware skips `POST /` (path empty → public, and methodToPermission('') is null). A readonly token (or, with C7, NO token) creates/updates/deletes anything via a transaction; a patient-scoped token reads anyone through batch GETs. PROBE: no token, readonly, patient-scoped batch GET.
  C25 Bundle PUT ifMatch compares the raw header to `W/"n"` as strings (a strong `"1"` → 412) and OUTSIDE the write (`saveResource(putResource)` has no `ifMatchVersion`): finding 30's fix did not reach the Bundle path; with ifMatch and no existing resource it silently creates. PROBE.
  C26 Batch/transaction entries with a search URL (`GET Patient?name=x`, conditional `DELETE Patient?identifier=`) are answered 400 "Invalid resource type: Patient?name=x": unsupported, and mis-diagnosed. Low.
  STILL OPEN from 2026-09-06 row 16: processing order / forward urn:uuid (not fixed; `_buildUrnUuidMap` maps only entries that already carry an id).
- compartment_handler.dart: `$everything` `_since` that does not parse is silently ignored (returns everything; should 400). Page hydration is N single reads (the IN read of 6.1 not applied here). `Patient/x/Patient`: check how the store scopes the focal type (DAO). Otherwise ok (scope on returned types, own-compartment check).
- history_handler.dart: candidates —
  C27 🔴 No patient-compartment check on ANY history route: a patient-scoped token reads `Observation/<other>/_history`, `_history/<vid>`, `Observation/_history` and `/_history` (every patient's versions). PROBE instance + type + system.
  C28 History Bundles carry no self/next/previous links: `_count`/`_offset` work but a client cannot page (R4B http.html history: paged like search). Low-medium; read the section before writing it up.
  `_since`/`_at` that do not parse are silently ignored (Low). 404 bodies are `{"error":…}` (C22 family).
- meta_handler.dart: ok.
- SPEC READ 2026-09-08 (R4B http.html §3.1.0.8 create, curl → scratchpad/http_r4b.txt, section printed whole): "If an id is provided, the server SHALL ignore it." and "The server SHALL populate the id, meta.versionId and meta.lastUpdated with the new correct values." → C20 confirmed by reading; probe still to write. Also: "SHALL also return a Location header which contains the new Logical Id and Version Id ... Location: [base]/[type]/[id]/_history/[vid] ... The Location header should be as specific as possible - if the server understands versioning, the version is included." fhirant sends `/Type/id` with no version (C30, low).
- patch_handler.dart: C31 PATCH is read-modify-write with no transaction and ignores If-Match (`saveResource(patchedResource)` without `ifMatchVersion`): a concurrent PUT between the read and the save is silently overwritten (finding 30's class, not fixed for PATCH). Under patient scope the same body check gap as C21.
- json_patch.dart: C32 `replace` on a member that does not exist ADDS it (RFC 6902 §4.3: "The target location MUST exist for the operation to be successful"); read RFC wording before writing up. Low.
- search_parser.dart: `_summary`/`_total` values not validated (unknown value → unchanged output). Low.
- search_links.dart: C33 `url()` passes `queryParameters: null` when nothing was used, and `Uri.replace(queryParameters: null)` KEEPS the original query: `GET /Patient?gendr=male` gets a self link echoing `gendr=male`, the exact 3.1.1.6 case the class exists for. PROBE.
- response_shaper.dart: C34 SUBSETTED is written to `meta.security`; check search.html 3.1.1.5.8 / an R4B example for whether it is `meta.tag`. VERIFY before writing.
- db_resource_cache.dart: `_seen` never invalidated (check lifetime at the call sites); `getResourceNames`/`getStructureDefinitions` decode every canonical of five types per call. Perf, check callers.
- filter_expression.dart: ok. filter_evaluator.dart: `eq` on a CHAINED string (`subject.name eq x`) passes as reference and runs starts-with (the unchained case is refused). Low.
- SPEC READ 2026-09-08 (R4B search.html 3.1.1.5.8 and 3.1.1.5.9, printed from scratchpad/search_r4b.txt): "Servers SHOULD mark the resources with the tag SUBSETTED"; resource.html and security-labels.html (R4B) contain no SUBSETTED at all; examples-json.zip has no instance carrying it. → C34: the code writes it to `meta.security`; the page says tag. Write up as BY READING; a probe shows the placement.
- authorize_handler.dart: candidates —
  C36 Open redirect: `response_type != code` (GET and POST) and a PKCE problem on a NEW client_id go to `_errorRedirect(redirectUri…)` before/without any pin: `GET /auth/authorize?response_type=token&client_id=new&redirect_uri=https://attacker&state=s` → 302 to the attacker. RFC 6749 §4.1.2.1: read the exact sentence before writing. PROBE.
  C37 `aud` is accepted and never validated; `state` is optional. SMART App Launch STU2 app-launch.html: read the `aud` and `state` rows verbatim before writing. Low-medium.
  ok: PKCE S256 required, redirect pinned after credentials, scope intersected, lockout shared, HTML escaped.
- refresh_handler.dart: C38 Deactivating an account (`active=false`) does not invalidate its live access tokens: authMiddleware never re-reads the account, so a deactivated user keeps working for up to 8 h; the lock (`locked_until`) likewise does not stop refresh. PROBE (deactivate, then GET with the old token → 200). Rotation without reuse detection (low). ok otherwise.
- register_handler.dart: ok (first-user race negligible on-device; usernames not normalised, low).
- login_handler.dart / unlockAccountHandler: role check present → C1 CLOSED (no finding).
- revoke_handler.dart: C39 `/auth/revoke` and `/auth/logout` insert a revoked_tokens row for ANY string, unauthenticated, under the general limiter only (600/min): a caller can grow the table without bound (retention = 24 h fallback). Low-medium on a phone. BY READING (check the DAO insert + cleanup interval).
- smart_config_handler.dart: C40 `registration_endpoint` points at `/auth/register`, the USER registration endpoint, not RFC 7591 dynamic client registration; a SMART client following the document posts client metadata there. Low. Read conformance.html's registration_endpoint row before writing.
- health_handler.dart / audit: C35 `/health` is not in `_shouldAudit`'s skip list → every health poll writes an AuditEvent as `anonymous`, unauthenticated (the same class as the bare-401 fix). Check how often the app polls it. PROBE by reading + count.
- websocket: any authenticated caller binds to any subscription id (pings carry no payload). Low, note only.
- metadata_handler.dart: C41 `Resource-fhirpath` OperationDefinition canonical: verify it exists in R4B (definitions.json.zip); websocket extension always `ws://` even under https; `cors: true` advertised while CORS is off by default. Low.
- SPEC READ 2026-09-08: SMART App Launch STU2 app-launch.html authorization request table (scratchpad/smart_app_launch.txt): `state` — "required … The parameter SHALL be used for preventing cross-site request forgery"; `aud` — "required … URL of the EHR resource server from which the app wishes to retrieve FHIR data. This parameter prevents leaking a genuine bearer token to a counterfeit resource server." RFC 6749 §4.1.2.1 (rfc6749.txt line 1492): "…MUST NOT automatically redirect the user-agent to the invalid redirection URI." → C36, C37 stand.
- export_handler.dart: candidates —
  C42 `_typeFilter` is applied through `dbInterface.search(...)` with NO `count`, so the filter's id set is ONE DEFAULT PAGE of matches (check FhirAntDb.search's default); an export with `_typeFilter=Observation?status=final` writes only the first page's worth of Observations. PROBE with 25+ matches.
  C43 Kick-off parameters the server does not implement (`patient`, `_elements`, `includeAssociatedData`) are parsed and silently ignored; export.html says what to do — read the "Query Parameters" section verbatim first.
  C44 `exportDeleteHandler` removes the job row while `_processExport` may still be running; the worker recreates the directory and writes files nobody owns until the hourly sweep. Low.
  Patient-level export exports every resource of the compartment TYPES, not the compartment MEMBERS (an Observation with no subject is exported). Low; note.
  ok: job id validated, path traversal blocked, streamed files, retention sweep, OperationOutcome on failure.
- backup_service.dart / backup_handler.dart / backup_crypto.dart: ok (PBKDF2 210k + AES-256-GCM, fresh salt/nonce; passphrase never in URL; first-byte sniffing; temp dir cleaned). Bundle-format backup is whole-in-memory by design (legacy).
- spec_loader.dart: ok.
- document_handler.dart: C14' `$document` has NO patient-compartment check and no scope check on the referenced types: a patient-scoped token reads any Composition and everything it references. PROBE. Closure is one level (direct references only): check documents.html before calling it a defect.
- SPEC READ 2026-09-08 Bulk Data 2.0.0 export.html "Query Parameters" (scratchpad/bulk_export.txt): "A server that is unable to support _elements SHOULD return an error and FHIR OperationOutcome resource so the client can re-submit a request omitting the _elements parameter. When a Prefer: handling=lenient header is included in the request, the server MAY process the request instead of returning an error." The same sentence for `patient` ("…omitting the patient parameter") and `includeAssociatedData`. → C43 confirmed BY READING.
- terminology_handler.dart: C45 `$validate-code` against a ValueSet COMPOSE returns result=true for a code that `compose.exclude.concept` removes (the include match returns before exclude is consulted; the comment at the end admits it); `$expand` and the DAO's `:in` do apply exclude. PROBE. Also: `version`/`systemVersion`/`activeOnly`/`includeDesignations` silently ignored (low); patient-scoped tokens reach CodeSystem/ValueSet operations though a direct GET is refused (harmless, note).
- cql_handler.dart (first half): candidates —
  C46 🔴 `Library/<id>/$evaluate` and `POST /Library/$evaluate` pass the middleware on 'r' for Library (patient/*.rs qualifies) and the handler applies no compartment check to `subject`: a patient-scoped token evaluates arbitrary inline CQL (`cql` parameter) over ANY patient's record. PROBE.
  C47 `_loadPatientData` (and C42's export filter) call `search(...)` with no `count`: settle what a null count means in FhirDao before writing either up; if it is a page, CQL runs on the first page of each type and answers WRONG silently.
- SETTLED 2026-09-08: FhirDao.search with `count == null` → `query.limit(-1, offset)` (fhir_dao.dart:549-552), i.e. NO page. C42 and C47 are NOT defects (the export `_typeFilter`, CQL `_loadPatientData`, forecast, `_resolveProfile` all get the whole set; memory only). Dropped.
- CONFIRMED BY READING: R4B profiles-resources.json defines no `OperationDefinition/Resource-fhirpath` (the list: CodeSystem-find-matches/lookup/subsumes/validate-code, Composition-document, ConceptMap-closure/translate, Library-data-requirements, NamingSystem-preferred-id, Patient-everything/match, Resource-convert/graph/graphql/meta/meta-add/meta-delete/validate, StructureMap-transform, ValueSet-expand/validate-code). → C41 stands: the CapabilityStatement names a canonical that does not exist.
- cql_handler.dart (rest): ok. Evaluation errors answer 500 (a bad CQL should be 400/422; low).
- mapping_handler.dart: C48 `$transform` takes a non-standard body `{map:…, source:…}`; OperationDefinition StructureMap-transform names `source` (uri) and `content` (resource) — verify the parameter list from profiles-resources.json before writing. Otherwise ok (target type resolved, ambiguity refused).
- forecast_handler.dart: Patient not found → 500 "Immunization forecast error" (should be 404). Low. Otherwise ok; root data op gated.
- validate_handler.dart: ok. fhirpath_handler.dart: parse errors → 500 (should be 400). Low.
- subscription_service.dart: C49 (policy) any account with `c` on Subscription (a clinician, `user/*.*`) can register a rest-hook to ANY URL with `payload` set and arbitrary `channel.header`, and the server then PUTs every matching resource (PHI) to that URL on every write, from inside the LAN (SSRF + persistent exfiltration). Not a bug in the mechanism (that is what rest-hook is) but the authorization boundary: Subscription create/update should be admin/system-scoped, or endpoints allow-listed. BY READING. Otherwise ok (queue, timeout, failure count, websocket ping).
- handlers.dart / utils.dart / lib exports: ok.
- CONFIRMED BY READING (R4B OperationDefinition StructureMap-transform): in `source` 0..1 uri, in `content` 1..1 Resource, out `return`. fhirant's `$transform` body is `{map: <StructureMap JSON>, source: <resource>}`: `source` means the RESOURCE here and the MAP there, and there is no `content`. → C48 stands (non-standard interface; a client following the OperationDefinition cannot call it).
- fhirant_db.dart: candidates —
  C50 `cleanupAuthorizationCodes()` exists but nothing calls it (grep below): `authorization_codes` grows by one row per authorize for ever. Low.
  C51 `restoreEncrypted` merges `users` with `INSERT OR IGNORE` on the AUTOINCREMENT id: on a replacement device that has bootstrapped its own admin (id 1), the backup's accounts with ids 1..n are silently dropped (and a username clash is dropped too). The documented "local wins" is right for a clash of the SAME account, wrong for different accounts sharing an id. PROBE (bootstrap admin, restore a backup with two accounts, count users).
  `logs` table created and (check) never written; `getResourcesByTypeSince` (check) unused since finding 34. `saveResource` failures go to stderr, not FhirantLogging. Timestamps: server tables in seconds (drift DateTime default), fhir tables in ms — consistent within each. ok otherwise (copy/restore exclude tagged rows incl. contained; cipher guard; schema upgrade of a backup in place).
- CONFIRMED (grep of fhirant lib/bin, 0 hits): `cleanupAuthorizationCodes` never called (C50); `getResourcesByTypeSince` dead; `logs` table created, never written (dead schema).
- fhir_db.dart: C52 `rebuildSearchIndex()` extracts with `updateSearchParameters(resource)`, not `extractWithContained`: if saveResource indexes contained resources (contained_index.dart), every schema rebuild (10; fhirant 14/17/18) DROPS the `#Type` rows of contained resources, so chained searches into contained resources answer nothing after an upgrade until the container is re-saved. Verify in FhirDao.saveResource, then PROBE (save MedicationRequest+contained Medication, chain search hits; rebuildSearchIndex; chain search empty).
  ok: migrations idempotent, ANALYZE policy measured, covering indexes, sentinels.
- cipher_from_key.dart: ok (unused by fhirant; `PRAGMA key = "x'hex'"` form). has_parameter.dart: ok. search_query_key.dart / search_escaping.dart / search_date_range.dart / implicit_range.dart / compartment_scope.dart / contained_index.dart / compartment_definitions.dart (generated): ok.
- tables: token/string/reference/date/quantity/number/uri/special/composite: ok on reading (open bounds as sentinels; name-part word rows; reference parse per references.html; uri as written).
- fhir_dao.dart: candidates —
  C52 CONFIRMED BY READING: `FhirDao.extractSearchParameters = extractWithContained` (line 50) but `FhirDb.rebuildSearchIndex` calls `updateSearchParameters(resource)` (fhir_db.dart:430): every rebuild drops the `#Type` rows of contained resources. PROBE.
  C53 🔴 `_searchMissingParameter` (fhir_dao.dart:4472-4480) runs raw SQL on `search_path`, a column the schema-10 tables no longer have (no table in tables/*.dart declares it): any `:missing` that reaches the general path (`_resolveSearchParameter` intercepts `missing` before the typed branches) is a SQL error → the server's 500. Reached when `_pagedIds` returns null: an `_id` list over 500 values, a chain the paged path cannot build, `:of-type`. PROBE: `GET /Patient?_id=<501 ids>&name:missing=true`.
  C54 A token value with two or more pipes (`a|b|c`) is taken as the LITERAL code `a|b|c` (`_tokenCondition`: `parts.length == 2` only) rather than refused as malformed. Low. `_referenceCondition` is fine.
  C55 `HistoryEntry.fromRow` recognises a tombstone by a `meta.tag` of v3-ObservationValue|DELETED: a client that stores a live resource carrying that tag makes its history read as deleted (vread 410, `_history` DELETE entry). Low; note only, check whether the code system even has DELETED.
  ok: saveResource/deleteResource in one transaction with If-Match inside; bulk save atomic with continued versions; _nextMeta per http.html/resource.html; paged SQL path with probes, walks, sort shapes; chain/has/compartment/contained; numeric and date ranges as one range each; ap/sa/eb per 3.1.1.4.5; `_id` >500 to the set path with existence check; hydration in IN chunks.
- fhir_r4_bulk: models/stream ok (a `_typeFilter` query's own commas survive the split only when the client double-encodes, as export.html's example does; note in the report). C56 client `BulkRequest._buildExportUrl` writes `_since=$since` unencoded: an offset `+05:00` becomes a space on the wire. Low (client side). `_type=Type/id` form is not in the IG (low).
- fhirant_secure_storage: ok (identity reused, fingerprint over DER, rejection sampling). fhirant_logging: ok as a sink. C57 (check with grep below) handlers log `Type/id`, `$everything for Patient/<id>`, the FHIRPath `expression`, and refused search messages (with values) through logInfo/logWarning: those reach the plaintext file when a log path is set, which is what `_redactUri` exists to prevent (SECURITY-REVIEW F6).

## App (packages/fhirant) and deployment files, read 2026-09-08
- A1 🟠 onboarding `_loadSampleData` sets `state.devMode = true` (persisting `auth_disabled=true` as if the operator chose it) and starts the server, though it then writes through `db.saveResources` directly and never uses the server. Loading sample data during onboarding silently commits the install to Experimentation mode. BY READING (onboarding_screen.dart:78-81, server_state.dart:97-104).
- A2 🟠 In Experimentation mode `/auth/register` is public and `getUserCount()==0` → first-user bootstrap: anyone on the WiFi registers the ADMIN account (dev-mode middleware still injects `auth_user`, but the register handler's bootstrap does not need it). Later the operator flips to Secure: `_onModeChanged` sees userCount>0, skips the admin dialog, and the stranger's account is the admin of Secure mode. PROBE (register in dev mode → count → secure-mode login with that account).
- A3 `_import` calls `FilePicker.pickFiles(withData: true)` and then uses only `.path`: file_picker loads the whole picked file into memory (`PlatformFile.bytes`) for nothing; a multi-GB SQLite backup on a phone is loaded into RAM before restore begins. Also `readAsString()` of the whole file for the JSON-envelope check (`isEncrypted`) reads a plain JSON Bundle into memory twice (once here, once in `restore`). BY READING (backup_card.dart:151-160). Measure: RSS of restoreFile on the MIMIC JSON bundle? The JSON path is `restore(db, String)` — in-memory by design for the legacy envelope; the SQLite path streams. Low/medium.
- A4 `_refreshResourceCounts` every 10 s = 1 DISTINCT + one COUNT per type, sequential, forever while running. MEASURE on the MIMIC copy (perf_load already measures 'count-refresh').
- A5 `_detectWifiIp` fallback takes the first non-loopback IPv4 of any interface (cellular, VPN, hotspot); the QR then advertises an address the LAN may not reach. Low.
- A6 Resource browser `_handleReferenceTap` opens any http(s) URL found in a stored resource in the external browser. Operator's own device; low.
- A7 INSTALL.md and RELEASE_NOTES.md say `$backup` "returns your whole dataset as a FHIR Bundle"; the handler now returns `application/vnd.sqlite3` (encrypted SQLite) and the Bundle is the legacy envelope only. Docs stale. Low.
- A8 Dockerfile runtime: runs as root (dart:stable default), no HEALTHCHECK, no `USER`; `test_docker.sh` uses `X-Forwarded-For` to dodge the limiter, which `_trustedClientIpMiddleware` now overwrites — the script's comment is stale and all its requests share one bucket (10/60 s on credential paths only, so it still passes). Low.
- Tool scripts (perf_*, review_2026-09-06/*): measurement only; `index_trim_ab.dart` leaves two production indexes dropped by design (documented). No production impact.
